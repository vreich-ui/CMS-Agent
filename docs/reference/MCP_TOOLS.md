# MCP tool reference (generated)

<!-- GENERATED by scripts/generateMcpToolReference.ts — do not edit by hand. Regenerate: npx tsx scripts/generateMcpToolReference.ts -->

Source of truth: `createWorkspaceTools` in `src/agent/mcp/workspace/tools.ts` (and the modules it composes). 151 tools, 24 namespaces, 3 deprecated aliases. The wire-surface lock is `docs/mcp-tool-manifest.json` (`npm run test:drift`).

How to read this file: **wire name** is what `tools/list` advertises and what remote connectors must call; the **internal** dotted name is also accepted by `tools/call`. **Effect** classes: `read` (no writes), `mutate-workspace` (workspace document / skills — change-history recorded), `mutate-run` (a run record), `mutate-registry` (project records), `mutate-learning` (evaluation / improvement / usage / observations), `execute-model` (spends model budget), `external-read` / `external-mutate` (reaches a project MCP or another external system), `publish` (writes a tenant's live site), `admin` (fleet / infrastructure). **Autonomy**: `safe` = an unattended agent may call it; `caution` = writes durable state, spends money, or reaches a tenant; `operator` = irreversible, fleet-wide, or production-affecting — a human should be in the loop. No MCP tool requires a human approval step inside CMS-Agent itself; approval semantics live in the publish gates (`docs/PUBLISHING_ARCHITECTURE.md`) and project tool policies (`needs_approval`). Authorization is per bearer, see `docs/MCP_ARCHITECTURE.md` (a static `MCP_API_TOKEN` or OAuth token can call every listed tool; a scoped bearer only its allowlist).

## Summary by namespace

| Namespace | Tools | read | mutating | execute-model / external | operator-only |
|---|---|---|---|---|---|
| `agent` | 5 | 3 | 1 | 1 | 0 |
| `changes` | 4 | 3 | 1 | 0 | 0 |
| `constellation` | 5 | 5 | 0 | 0 | 0 |
| `dataset` | 6 | 5 | 1 | 0 | 0 |
| `evaluation` | 11 | 6 | 3 | 2 | 0 |
| `feedback` | 4 | 1 | 1 | 2 | 0 |
| `learning` | 4 | 1 | 3 | 0 | 0 |
| `node` | 15 | 12 | 1 | 2 | 0 |
| `optimizer` | 6 | 2 | 2 | 2 | 2 |
| `playbook` | 4 | 1 | 3 | 0 | 0 |
| `project` | 11 | 4 | 3 | 4 | 3 |
| `publish` | 2 | 1 | 0 | 1 | 0 |
| `repository` | 1 | 1 | 0 | 0 | 0 |
| `site` | 2 | 1 | 0 | 1 | 1 |
| `site_credentials_apply` | 1 | 0 | 0 | 1 | 1 |
| `site_credentials_execution_status` | 1 | 0 | 0 | 1 | 0 |
| `site_credentials_plan` | 1 | 1 | 0 | 0 | 0 |
| `skill` | 13 | 6 | 7 | 0 | 0 |
| `stage` | 3 | 2 | 1 | 0 | 0 |
| `tool` | 6 | 5 | 0 | 1 | 0 |
| `usage` | 4 | 3 | 1 | 0 | 0 |
| `visual_identity` | 1 | 0 | 0 | 1 | 0 |
| `workflow` | 18 | 5 | 7 | 6 | 2 |
| `workspace` | 23 | 7 | 16 | 0 | 5 |

## Deprecated aliases (accepted by tools/call, never listed)

| Alias (dotted / wire) | Resolves to |
|---|---|
| `node.list` / `node_list` | `workspace_get_nodes` |
| `node.get_execution` / `node_get_execution` | `node_list_executions` |
| `workspace.update_node_schema` / `workspace_update_node_schema` | `workspace_update_node_output_schema` |

## Tools

### `agent`

| Wire name | Effect | Storage written | External reach | Autonomy | Note |
|---|---|---|---|---|---|
| [`agent_converse`](#agent_converse) | execute-model | conversation-turn-claims/, conversations/, usage/ | model provider (exactly one request) | caution | Idempotent on (conversation_id, turn_id). Returns tool calls as proposals; never executes them. |
| [`agent_get`](#agent_get) | read | - | - | safe |  |
| [`agent_list`](#agent_list) | read | - | - | safe |  |
| [`agent_resolve`](#agent_resolve) | read | - | - | safe |  |
| [`agent_update`](#agent_update) | mutate-workspace | workspace/current.json (conversationalAgents), changes/, revisions/ | - | caution |  |

#### `agent_converse`

Internal name: `agent.converse` · Effect: **execute-model** · Autonomy: **caution**

This is the raw single-turn primitive used BY the site admin chat; humans and editor-agents should use the admin chat, not call this directly. Execute exactly one client_manager.turn.v1 model turn. Caller tools are passed to the provider and returned as unexecuted tool calls; CMS-Agent never executes them or owns the human wait state. Duplicate conversation_id + turn_id calls replay one stored response without another provider call.

> Idempotent on (conversation_id, turn_id). Returns tool calls as proposals; never executes them.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "additionalProperties": false,
  "required": [
    "agent_ref",
    "project_id",
    "conversation_id",
    "turn_id",
    "actor",
    "context",
    "messages",
    "tools",
    "constraints"
  ],
  "properties": {
    "agent_ref": {
      "type": "string",
      "minLength": 1,
      "maxLength": 256
    },
    "project_id": {
      "type": "string",
      "minLength": 1,
      "maxLength": 63
    },
    "conversation_id": {
      "type": "string",
      "minLength": 1,
      "maxLength": 256
    },
    "turn_id": {
      "type": "string",
      "minLength": 1,
      "maxLength": 256
    },
    "actor": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "kind",
        "id"
      ],
      "properties": {
        "kind": {
          "type": "string",
          "const": "human"
        },
        "id": {
          "type": "string",
          "minLength": 1,
          "maxLength": 256
        }
      }
    },
    "context": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "site_id"
      ],
      "properties": {
        "site_id": {
          "type": "string",
          "minLength": 1,
          "maxLength": 128
        },
        "object_type": {
          "type": "string",
          "minLength": 1,
          "maxLength": 128
        },
        "object_id": {
          "type": "string",
          "minLength": 1,
          "maxLength": 256
        },
        "focus": {
          "type": "string",
          "minLength": 1,
          "maxLength": 500
        },
        "learning_mode": {
          "type": "boolean"
        },
        "diagnostics_requested": {
          "type": "boolean"
        },
        "approval_note": {
          "type": "string",
          "minLength": 1,
          "maxLength": 1000
        }
      }
    },
    "messages": {
      "type": "array",
      "minItems": 1,
      "maxItems": 200,
      "items": {
        "oneOf": [
          {
            "type": "object",
            "additionalProperties": false,
            "required": [
              "role",
              "text"
            ],
            "properties": {
              "role": {
                "type": "string",
                "const": "user"
              },
              "text": {
                "type": "string"
              }
            }
          },
          {
            "type": "object",
            "additionalProperties": false,
            "required": [
              "role"
            ],
            "properties": {
              "role": {
                "type": "string",
                "const": "assistant"
              },
              "text": {
                "type": "string"
              },
              "tool_calls": {
                "type": "array",
                "items": {
                  "type": "object",
                  "additionalProperties": false,
                  "required": [
                    "id",
                    "name",
                    "args"
                  ],
                  "properties": {
                    "id": {
                      "type": "string"
                    },
                    "name": {
                      "type": "string"
                    },
                    "args": {
                      "type": "object"
                    }
                  }
                }
              }
            }
          },
          {
            "type": "object",
            "additionalProperties": false,
            "required": [
              "role",
              "tool_call_id",
              "content"
            ],
            "properties": {
              "role": {
                "type": "string",
                "const": "tool"
              },
              "tool_call_id": {
                "type": "string"
              },
              "content": {
                "type": "string"
              },
              "is_error": {
                "type": "boolean"
              }
            }
          }
        ]
      }
    },
    "tools": {
      "type": "array",
      "maxItems": 96,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "name",
          "description",
          "input_schema"
        ],
        "properties": {
          "name": {
            "type": "string"
          },
          "description": {
            "type": "string"
          },
          "input_schema": {
            "type": "object"
          }
        }
      }
    },
    "constraints": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "max_tokens",
        "timeout_ms"
      ],
      "properties": {
        "max_tokens": {
          "type": "integer",
          "minimum": 1,
          "maximum": 32000
        },
        "timeout_ms": {
          "type": "integer",
          "minimum": 1000,
          "maximum": 120000
        }
      }
    }
  }
}
```

</details>

#### `agent_get`

Internal name: `agent.get` · Effect: **read** · Autonomy: **safe**

Read one conversational agent definition, including its full prompt. Read-only.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^agt_[a-z0-9_]+$"
    }
  },
  "required": [
    "id"
  ],
  "additionalProperties": false
}
```

</details>

#### `agent_list`

Internal name: `agent.list` · Effect: **read** · Autonomy: **safe**

List conversational agent definitions with their prompt, model configuration, status and revision. Read-only. promptState reports whether the stored prompt is the shipped canonical text, an older shipped text, or an operator edit.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {},
  "required": [],
  "additionalProperties": false
}
```

</details>

#### `agent_resolve`

Internal name: `agent.resolve` · Effect: **read** · Autonomy: **safe**

Resolve the active conversational agent for a registered project and role. Returns an opaque agent reference and definition revision; callers must not hardcode agent or node ids.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "role": {
      "type": "string",
      "const": "client_manager"
    },
    "project_id": {
      "type": "string",
      "minLength": 1,
      "maxLength": 63
    }
  },
  "required": [
    "role",
    "project_id"
  ],
  "additionalProperties": false
}
```

</details>

#### `agent_update`

Internal name: `agent.update` · Effect: **mutate-workspace** · Autonomy: **caution**

Update a conversational agent's name, prompt, model configuration, skills or status. Guarded by expectedWorkspaceVersion like every workspace write; bumps the definition revision, which invalidates outstanding agent_ref values so callers re-resolve. Ledgered as agent.updated with before/after.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^agt_[a-z0-9_]+$"
    },
    "patch": {
      "type": "object"
    },
    "expectedWorkspaceVersion": {
      "type": "integer",
      "minimum": 0
    },
    "baseRevisionId": {
      "type": "string"
    },
    "actor": {
      "oneOf": [
        {
          "type": "string",
          "minLength": 1
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "enum": [
                "human",
                "agent",
                "system"
              ]
            },
            "id": {
              "type": "string"
            },
            "label": {
              "type": "string"
            }
          }
        }
      ]
    },
    "source": {
      "type": "string",
      "enum": [
        "mcp",
        "ui",
        "system"
      ]
    },
    "summary": {
      "type": "string"
    },
    "reason": {
      "type": "string"
    },
    "correlation": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "runId": {
          "type": "string"
        },
        "requestId": {
          "type": "string"
        }
      }
    }
  },
  "required": [
    "id",
    "patch"
  ],
  "additionalProperties": false
}
```

</details>

### `changes`

| Wire name | Effect | Storage written | External reach | Autonomy | Note |
|---|---|---|---|---|---|
| [`changes_compare`](#changes_compare) | read | - | - | safe |  |
| [`changes_get`](#changes_get) | read | - | - | safe |  |
| [`changes_list`](#changes_list) | read | - | - | safe |  |
| [`changes_restore`](#changes_restore) | mutate-workspace | workspace/current.json, changes/, revisions/ | - | caution | Restore is a new revision; history is never deleted. |

#### `changes_compare`

Internal name: `changes.compare` · Effect: **read** · Autonomy: **safe**

Compare two workspace revisions and return a node-level diff plus a relationships diff.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "fromRevisionId": {
      "type": "string",
      "minLength": 1
    },
    "toRevisionId": {
      "type": "string",
      "minLength": 1
    },
    "detail": {
      "type": "string",
      "enum": [
        "summary",
        "full"
      ],
      "default": "summary",
      "description": "summary (default): event headers, changed field names and before/after byte counts. full: the complete before/after snapshots."
    }
  },
  "required": [
    "fromRevisionId",
    "toRevisionId"
  ],
  "additionalProperties": false
}
```

</details>

#### `changes_get`

Internal name: `changes.get` · Effect: **read** · Autonomy: **safe**

Get one immutable workspace change event by id.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "eventId": {
      "type": "string",
      "minLength": 1
    }
  },
  "required": [
    "eventId"
  ],
  "additionalProperties": false
}
```

</details>

#### `changes_list`

Internal name: `changes.list` · Effect: **read** · Autonomy: **safe**

List immutable workspace change events, newest first, with pagination and filters for a future Changes UI.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "detail": {
      "type": "string",
      "enum": [
        "summary",
        "full"
      ],
      "default": "summary",
      "description": "summary (default): event headers, changed field names and before/after byte counts. full: the complete before/after snapshots."
    },
    "nodeId": {
      "type": "string",
      "minLength": 1
    },
    "operation": {
      "type": "string",
      "enum": [
        "create",
        "update",
        "delete",
        "clone",
        "reorder",
        "restore",
        "import",
        "record"
      ]
    },
    "actorKind": {
      "type": "string",
      "enum": [
        "human",
        "agent",
        "system"
      ]
    },
    "source": {
      "type": "string",
      "enum": [
        "mcp",
        "ui",
        "system"
      ]
    },
    "from": {
      "type": "string",
      "format": "date-time"
    },
    "to": {
      "type": "string",
      "format": "date-time"
    },
    "limit": {
      "type": "integer",
      "minimum": 1,
      "maximum": 200,
      "default": 50
    },
    "cursor": {
      "type": "string",
      "minLength": 1
    }
  },
  "required": [],
  "additionalProperties": false
}
```

</details>

#### `changes_restore`

Internal name: `changes.restore` · Effect: **mutate-workspace** · Autonomy: **caution**

Restore one node from a historical revision as a NEW revision. History is append-only and is never rewritten or deleted.

> Restore is a new revision; history is never deleted.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "revisionId": {
      "type": "string",
      "minLength": 1
    },
    "nodeId": {
      "type": "string",
      "minLength": 1
    },
    "expectedWorkspaceVersion": {
      "type": "integer",
      "minimum": 0
    },
    "baseRevisionId": {
      "type": "string"
    },
    "actor": {
      "oneOf": [
        {
          "type": "string",
          "minLength": 1
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "enum": [
                "human",
                "agent",
                "system"
              ]
            },
            "id": {
              "type": "string"
            },
            "label": {
              "type": "string"
            }
          }
        }
      ]
    },
    "source": {
      "type": "string",
      "enum": [
        "mcp",
        "ui",
        "system"
      ]
    },
    "summary": {
      "type": "string"
    },
    "reason": {
      "type": "string"
    },
    "correlation": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "runId": {
          "type": "string"
        },
        "requestId": {
          "type": "string"
        }
      }
    }
  },
  "required": [
    "revisionId",
    "nodeId"
  ],
  "additionalProperties": false
}
```

</details>

### `constellation`

| Wire name | Effect | Storage written | External reach | Autonomy | Note |
|---|---|---|---|---|---|
| [`constellation_get_attention`](#constellation_get_attention) | read | - | - | safe |  |
| [`constellation_get_metrics`](#constellation_get_metrics) | read | - | - | safe | Full-fleet run scan. |
| [`constellation_get_relationship`](#constellation_get_relationship) | read | - | - | safe |  |
| [`constellation_get_structure`](#constellation_get_structure) | read | - | - | safe |  |
| [`constellation_get_summary`](#constellation_get_summary) | read | - | - | safe |  |

#### `constellation_get_attention`

Internal name: `constellation.get_attention` · Effect: **read** · Autonomy: **safe**

Attention items with explicit evidence-citing reasons: failed runs, pending approvals, output-validation failures, pricing caveats, relationship issues, and (R-10) configuration defects — blocker-severity skill conflicts, skills requesting tools the node denies, dependsOn/requiredInputs disagreements, tools a node grants itself above its own risk level, and active client connections whose environment variables are unconfigured. No composite scores. Read-only.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "projectId": {
      "type": "string",
      "minLength": 1
    }
  },
  "required": [],
  "additionalProperties": false
}
```

</details>

#### `constellation_get_metrics`

Internal name: `constellation.get_metrics` · Effect: **read** · Autonomy: **safe**

Aggregated per-agent operational metrics (usage split by estimated/actual, executions, latency, derived retries, human intervention, output validation failures, current-process tool errors). Read-only; never billing-grade.

> Full-fleet run scan.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "projectId": {
      "type": "string",
      "minLength": 1
    },
    "runId": {
      "type": "string",
      "minLength": 1
    },
    "from": {
      "type": "string",
      "format": "date-time"
    },
    "to": {
      "type": "string",
      "format": "date-time"
    }
  },
  "required": [],
  "additionalProperties": false
}
```

</details>

#### `constellation_get_relationship`

Internal name: `constellation.get_relationship` · Effect: **read** · Autonomy: **safe**

One relationship (stored, or a derived execution edge addressed by source/target) with its derived interaction metrics. Read-only.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "relationshipId": {
      "type": "string",
      "minLength": 1
    },
    "sourceId": {
      "type": "string",
      "minLength": 1
    },
    "targetId": {
      "type": "string",
      "minLength": 1
    }
  },
  "required": [],
  "additionalProperties": false
}
```

</details>

#### `constellation_get_structure`

Internal name: `constellation.get_structure` · Effect: **read** · Autonomy: **safe**

Constellation structural data: agent summaries, stored typed relationships, and execution edges derived from node.dependsOn. Read-only.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {},
  "required": [],
  "additionalProperties": false
}
```

</details>

#### `constellation_get_summary`

Internal name: `constellation.get_summary` · Effect: **read** · Autonomy: **safe**

System summary for the constellation: agent/relationship/run counts and usage totals split by estimated/actual, with explicit caveats. Read-only.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "projectId": {
      "type": "string",
      "minLength": 1
    },
    "from": {
      "type": "string",
      "format": "date-time"
    },
    "to": {
      "type": "string",
      "format": "date-time"
    }
  },
  "required": [],
  "additionalProperties": false
}
```

</details>

### `dataset`

| Wire name | Effect | Storage written | External reach | Autonomy | Note |
|---|---|---|---|---|---|
| [`dataset_build`](#dataset_build) | mutate-learning | improvement/datasets | - | caution |  |
| [`dataset_export_preferences`](#dataset_export_preferences) | read | - | - | safe |  |
| [`dataset_export_sft`](#dataset_export_sft) | read | - | - | safe |  |
| [`dataset_finetune_readiness`](#dataset_finetune_readiness) | read | - | - | safe |  |
| [`dataset_get`](#dataset_get) | read | - | - | safe |  |
| [`dataset_list`](#dataset_list) | read | - | - | safe |  |

#### `dataset_build`

Internal name: `dataset.build` · Effect: **mutate-learning** · Autonomy: **caution**

Freeze a replay dataset for a node from completed historical executions (inputs + champion outputs) for offline champion/challenger trials. Each case records the execution mode it came from; pass executionMode:"openai" to freeze REAL champions only — a mock run's champion output is a deterministic schema-derived placeholder, not a champion. Cases are hashed by replay SUBJECT (input + dependency outputs + conductor-injected context) and identical subjects are dropped, because they re-execute to the same output and score identically; the result reports distinctSubjects and flags degenerate:true when fewer than two distinct subjects survive — a dataset with no discriminating power, whose regression verdicts look exactly like real ones.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "nodeId": {
      "type": "string",
      "minLength": 1
    },
    "name": {
      "type": "string"
    },
    "limit": {
      "type": "integer",
      "minimum": 1,
      "maximum": 100
    },
    "projectId": {
      "type": "string"
    },
    "executionMode": {
      "type": "string",
      "enum": [
        "mock",
        "openai"
      ],
      "description": "Only freeze cases from runs of this mode. Omit to include both (each case still reports its own sourceExecutionMode)."
    },
    "allowDuplicateSubjects": {
      "type": "boolean",
      "description": "Keep cases whose replay subject is identical to one already frozen (default false). They add cost and no signal; duplicateSubjects is reported either way."
    }
  },
  "required": [
    "nodeId"
  ],
  "additionalProperties": false
}
```

</details>

#### `dataset_export_preferences`

Internal name: `dataset.export_preferences` · Effect: **read** · Autonomy: **safe**

Export chosen/rejected preference pairs from decisive pairwise trial verdicts (DPO/ORPO-ready JSONL); inconsistent verdicts are excluded and counted.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "nodeId": {
      "type": "string",
      "minLength": 1
    },
    "limit": {
      "type": "integer",
      "minimum": 1,
      "maximum": 500
    }
  },
  "required": [
    "nodeId"
  ],
  "additionalProperties": false
}
```

</details>

#### `dataset_export_sft`

Internal name: `dataset.export_sft` · Effect: **read** · Autonomy: **safe**

Export judge-approved traces for a node as chat-format SFT JSONL (Vertex tuning / Unsloth compatible), with provenance metadata.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "nodeId": {
      "type": "string",
      "minLength": 1
    },
    "minScore": {
      "type": "number",
      "minimum": 0,
      "maximum": 1
    },
    "limit": {
      "type": "integer",
      "minimum": 1,
      "maximum": 500
    }
  },
  "required": [
    "nodeId"
  ],
  "additionalProperties": false
}
```

</details>

#### `dataset_finetune_readiness`

Internal name: `dataset.finetune_readiness` · Effect: **read** · Autonomy: **safe**

Fine-tuning flywheel trigger (DIRECTION Phase 8): report whether a node has accumulated enough approved SFT examples and decisive preference pairs to warrant a tuning run (thresholds via IMPROVEMENT_FINETUNE_MIN_EXAMPLES / IMPROVEMENT_FINETUNE_MIN_PREFERENCE_PAIRS). REPORT-ONLY — never launches a job; when ready, pair with dataset.export_sft / dataset.export_preferences. Optional minScore mirrors the export bar.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "nodeId": {
      "type": "string",
      "minLength": 1
    },
    "minScore": {
      "type": "number",
      "minimum": 0,
      "maximum": 1,
      "description": "Approve outputs at/above this normalized score instead of the rubric pass flag."
    }
  },
  "required": [
    "nodeId"
  ],
  "additionalProperties": false
}
```

</details>

#### `dataset_get`

Internal name: `dataset.get` · Effect: **read** · Autonomy: **safe**

Get one replay dataset.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "datasetId": {
      "type": "string",
      "minLength": 1
    }
  },
  "required": [
    "datasetId"
  ],
  "additionalProperties": false
}
```

</details>

#### `dataset_list`

Internal name: `dataset.list` · Effect: **read** · Autonomy: **safe**

List replay datasets.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "nodeId": {
      "type": "string"
    }
  },
  "required": [],
  "additionalProperties": false
}
```

</details>

### `evaluation`

| Wire name | Effect | Storage written | External reach | Autonomy | Note |
|---|---|---|---|---|---|
| [`evaluation_create_rubric`](#evaluation_create_rubric) | mutate-learning | evaluation/rubrics, evaluation/rubric-versions | - | caution |  |
| [`evaluation_get_result`](#evaluation_get_result) | read | - | - | safe |  |
| [`evaluation_get_rubric`](#evaluation_get_rubric) | read | - | - | safe |  |
| [`evaluation_list_regression_reports`](#evaluation_list_regression_reports) | read | - | - | safe |  |
| [`evaluation_list_results`](#evaluation_list_results) | read | - | - | safe |  |
| [`evaluation_list_rubric_versions`](#evaluation_list_rubric_versions) | read | - | - | safe |  |
| [`evaluation_list_rubrics`](#evaluation_list_rubrics) | read | - | - | safe |  |
| [`evaluation_restore_rubric_version`](#evaluation_restore_rubric_version) | mutate-learning | evaluation/rubrics | - | caution |  |
| [`evaluation_run`](#evaluation_run) | execute-model | evaluation/results, usage/ | judge model provider | caution |  |
| [`evaluation_run_regression`](#evaluation_run_regression) | execute-model | evaluation/regression, evaluation/results, runs/ (trial_ runs), usage/ | model provider (replay + judge) | caution |  |
| [`evaluation_update_rubric`](#evaluation_update_rubric) | mutate-learning | evaluation/rubrics, evaluation/rubric-versions | - | caution |  |

#### `evaluation_create_rubric`

Internal name: `evaluation.create_rubric` · Effect: **mutate-learning** · Autonomy: **caution**

Create a versioned, role-specific evaluation rubric for a node (criteria, weights, pass threshold, optional cross-family judge model).

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "rubric": {
      "type": "object"
    },
    "expectedWorkspaceVersion": {
      "type": "integer",
      "minimum": 0
    },
    "baseRevisionId": {
      "type": "string"
    },
    "actor": {
      "oneOf": [
        {
          "type": "string",
          "minLength": 1
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "enum": [
                "human",
                "agent",
                "system"
              ]
            },
            "id": {
              "type": "string"
            },
            "label": {
              "type": "string"
            }
          }
        }
      ]
    },
    "source": {
      "type": "string",
      "enum": [
        "mcp",
        "ui",
        "system"
      ]
    },
    "summary": {
      "type": "string"
    },
    "reason": {
      "type": "string"
    },
    "correlation": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "runId": {
          "type": "string"
        },
        "requestId": {
          "type": "string"
        }
      }
    }
  },
  "required": [
    "rubric"
  ],
  "additionalProperties": false
}
```

</details>

#### `evaluation_get_result`

Internal name: `evaluation.get_result` · Effect: **read** · Autonomy: **safe**

Get one evaluation result.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "evalId": {
      "type": "string",
      "minLength": 1
    }
  },
  "required": [
    "evalId"
  ],
  "additionalProperties": false
}
```

</details>

#### `evaluation_get_rubric`

Internal name: `evaluation.get_rubric` · Effect: **read** · Autonomy: **safe**

Get one rubric.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "rubricId": {
      "type": "string",
      "minLength": 1
    }
  },
  "required": [
    "rubricId"
  ],
  "additionalProperties": false
}
```

</details>

#### `evaluation_list_regression_reports`

Internal name: `evaluation.list_regression_reports` · Effect: **read** · Autonomy: **safe**

List stored regression-gate reports (newest first). The newest for a node is the baseline the next evaluation.run_regression compares against.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "nodeId": {
      "type": "string"
    },
    "limit": {
      "type": "integer",
      "minimum": 1,
      "maximum": 100
    }
  },
  "required": [],
  "additionalProperties": false
}
```

</details>

#### `evaluation_list_results`

Internal name: `evaluation.list_results` · Effect: **read** · Autonomy: **safe**

List evaluation results, newest first.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "nodeId": {
      "type": "string"
    },
    "runId": {
      "type": "string"
    },
    "rubricId": {
      "type": "string"
    },
    "trialId": {
      "type": "string"
    },
    "from": {
      "type": "string",
      "format": "date-time"
    },
    "to": {
      "type": "string",
      "format": "date-time"
    },
    "limit": {
      "type": "integer",
      "minimum": 1,
      "maximum": 200
    }
  },
  "required": [],
  "additionalProperties": false
}
```

</details>

#### `evaluation_list_rubric_versions`

Internal name: `evaluation.list_rubric_versions` · Effect: **read** · Autonomy: **safe**

List a rubric's version snapshots.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "rubricId": {
      "type": "string",
      "minLength": 1
    }
  },
  "required": [
    "rubricId"
  ],
  "additionalProperties": false
}
```

</details>

#### `evaluation_list_rubrics`

Internal name: `evaluation.list_rubrics` · Effect: **read** · Autonomy: **safe**

List rubrics, optionally by node and status.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "nodeId": {
      "type": "string"
    },
    "status": {
      "type": "string",
      "enum": [
        "draft",
        "active",
        "deprecated"
      ]
    }
  },
  "required": [],
  "additionalProperties": false
}
```

</details>

#### `evaluation_restore_rubric_version`

Internal name: `evaluation.restore_rubric_version` · Effect: **mutate-learning** · Autonomy: **caution**

Restore a rubric to a prior version (forward operation, snapshots again).

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "rubricId": {
      "type": "string",
      "minLength": 1
    },
    "versionId": {
      "type": "string",
      "minLength": 1
    },
    "expectedWorkspaceVersion": {
      "type": "integer",
      "minimum": 0
    },
    "baseRevisionId": {
      "type": "string"
    },
    "actor": {
      "oneOf": [
        {
          "type": "string",
          "minLength": 1
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "enum": [
                "human",
                "agent",
                "system"
              ]
            },
            "id": {
              "type": "string"
            },
            "label": {
              "type": "string"
            }
          }
        }
      ]
    },
    "source": {
      "type": "string",
      "enum": [
        "mcp",
        "ui",
        "system"
      ]
    },
    "summary": {
      "type": "string"
    },
    "reason": {
      "type": "string"
    },
    "correlation": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "runId": {
          "type": "string"
        },
        "requestId": {
          "type": "string"
        }
      }
    }
  },
  "required": [
    "rubricId",
    "versionId"
  ],
  "additionalProperties": false
}
```

</details>

#### `evaluation_run`

Internal name: `evaluation.run` · Effect: **execute-model** · Autonomy: **caution**

Score a node output against its rubric with the LLM judge (mode=openai) or the deterministic mock judge. Supply output inline or reference a run to score its recorded stage output.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "nodeId": {
      "type": "string",
      "minLength": 1
    },
    "rubricId": {
      "type": "string"
    },
    "runId": {
      "type": "string"
    },
    "output": {},
    "mode": {
      "type": "string",
      "enum": [
        "mock",
        "openai"
      ],
      "default": "mock"
    }
  },
  "required": [
    "nodeId"
  ],
  "additionalProperties": false
}
```

</details>

#### `evaluation_run_regression`

Internal name: `evaluation.run_regression` · Effect: **execute-model** · Autonomy: **caution**

Pre-ship regression gate for a node: re-run the node over a frozen replay dataset and rubric-score each output (mode=openai LLM judge or deterministic mock), then compare the aggregate to the node's last stored baseline. Returns a RegressionReport with per-case pass/fail, an absolute health GATE against the rubric's own passThreshold (gate pass|fail plus every gateReason: mean_below_threshold, cases_failed, cases_errored, no_cases_scored), the DRIFT against the node's last stored baseline in the same mode (baseline_set | improved | held | regressed), and a headline verdict that is "failing" whenever the gate fails and the drift value otherwise — so a node that scores below its threshold can never read as healthy just because it has not moved. Summary accounting is disjoint: casesPassed + casesFailed = casesScored, casesScored + casesErrored = casesTotal. REPORTS ONLY — never applies, promotes, or publishes; promotion stays optimizer.promote / the human path.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "nodeId": {
      "type": "string",
      "minLength": 1
    },
    "datasetId": {
      "type": "string",
      "description": "Frozen dataset to replay; omit to use the node's newest dataset or freeze one from history."
    },
    "rubricId": {
      "type": "string",
      "description": "Rubric to score against; omit to use the node's active rubric."
    },
    "mode": {
      "type": "string",
      "enum": [
        "mock",
        "openai"
      ],
      "default": "mock"
    },
    "caseLimit": {
      "type": "integer",
      "minimum": 1,
      "maximum": 100
    }
  },
  "required": [
    "nodeId"
  ],
  "additionalProperties": false
}
```

</details>

#### `evaluation_update_rubric`

Internal name: `evaluation.update_rubric` · Effect: **mutate-learning** · Autonomy: **caution**

Patch a rubric; every change snapshots a restorable version.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "rubricId": {
      "type": "string",
      "minLength": 1
    },
    "patch": {
      "type": "object"
    },
    "expectedWorkspaceVersion": {
      "type": "integer",
      "minimum": 0
    },
    "baseRevisionId": {
      "type": "string"
    },
    "actor": {
      "oneOf": [
        {
          "type": "string",
          "minLength": 1
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "enum": [
                "human",
                "agent",
                "system"
              ]
            },
            "id": {
              "type": "string"
            },
            "label": {
              "type": "string"
            }
          }
        }
      ]
    },
    "source": {
      "type": "string",
      "enum": [
        "mcp",
        "ui",
        "system"
      ]
    },
    "summary": {
      "type": "string"
    },
    "reason": {
      "type": "string"
    },
    "correlation": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "runId": {
          "type": "string"
        },
        "requestId": {
          "type": "string"
        }
      }
    }
  },
  "required": [
    "rubricId",
    "patch"
  ],
  "additionalProperties": false
}
```

</details>

### `feedback`

| Wire name | Effect | Storage written | External reach | Autonomy | Note |
|---|---|---|---|---|---|
| [`feedback_ingest_monetizer`](#feedback_ingest_monetizer) | external-read | evaluation/feedback | Monetizer MCP read tools | safe |  |
| [`feedback_ingest_tracking`](#feedback_ingest_tracking) | external-read | evaluation/feedback | tracking sink HTTPS | safe |  |
| [`feedback_list`](#feedback_list) | read | - | - | safe |  |
| [`feedback_record`](#feedback_record) | mutate-learning | evaluation/feedback | - | caution |  |

#### `feedback_ingest_monetizer`

Internal name: `feedback.ingest_monetizer` · Effect: **external-read** · Autonomy: **safe**

Outer-loop ingestion (DIRECTION Phase 7): pull the Monetizer project's read-only performance / demand_signals telemetry and record each as a feedback OUTCOME (source monetizer:<signal>), so published-content analytics feed optimizer.analyze. Optionally attribute to a nodeId/runId and pass Monetizer query args. Requires the Monetizer connection (MONETIZER_MCP_ENDPOINT / MONETIZER_MCP_TOKEN). Best-effort per signal; nothing external is written.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "nodeId": {
      "type": "string"
    },
    "runId": {
      "type": "string"
    },
    "signals": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": [
          "performance",
          "demand_signals"
        ]
      },
      "description": "Which signals to pull (default both)."
    },
    "args": {
      "type": "object",
      "description": "Query args forwarded to the Monetizer tool."
    },
    "note": {
      "type": "string"
    },
    "expectedWorkspaceVersion": {
      "type": "integer",
      "minimum": 0
    },
    "baseRevisionId": {
      "type": "string"
    },
    "actor": {
      "oneOf": [
        {
          "type": "string",
          "minLength": 1
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "enum": [
                "human",
                "agent",
                "system"
              ]
            },
            "id": {
              "type": "string"
            },
            "label": {
              "type": "string"
            }
          }
        }
      ]
    },
    "source": {
      "type": "string",
      "enum": [
        "mcp",
        "ui",
        "system"
      ]
    },
    "summary": {
      "type": "string"
    },
    "reason": {
      "type": "string"
    },
    "correlation": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "runId": {
          "type": "string"
        },
        "requestId": {
          "type": "string"
        }
      }
    }
  },
  "required": [],
  "additionalProperties": false
}
```

</details>

#### `feedback_ingest_tracking`

Internal name: `feedback.ingest_tracking` · Effect: **external-read** · Autonomy: **safe**

Outer-loop ingestion (T21.7, sibling of feedback.ingest_monetizer): pull the tracking sink's per-producer engagement rollups for a project/window and record each row as a feedback OUTCOME (source tracking:engagement.v1, metrics pageviews/exposures/sessions/completion_rate/cta_ctr/purchase_rate/revenue_cents/p75_dwell_ms), so published-content engagement feeds optimizer.analyze. Rows are attributed to the producing nodeId/runId; pass nodeId to restrict the pull to one node. Requires the tracking sink connection (TRACKING_SINK_URL / TRACKING_SINK_TOKEN). Best-effort and read-only against the sink; nothing external is written.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "projectId": {
      "type": "string",
      "minLength": 1,
      "description": "Tracking partition to read (the sink's TRACKING_PROJECT_ID)."
    },
    "from": {
      "type": "string",
      "minLength": 1,
      "description": "Window start (ISO date or date-time)."
    },
    "to": {
      "type": "string",
      "minLength": 1,
      "description": "Window end (ISO date or date-time)."
    },
    "nodeId": {
      "type": "string",
      "description": "Restrict to one producer node; also the attribution fallback for rows that omit one."
    },
    "expectedWorkspaceVersion": {
      "type": "integer",
      "minimum": 0
    },
    "baseRevisionId": {
      "type": "string"
    },
    "actor": {
      "oneOf": [
        {
          "type": "string",
          "minLength": 1
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "enum": [
                "human",
                "agent",
                "system"
              ]
            },
            "id": {
              "type": "string"
            },
            "label": {
              "type": "string"
            }
          }
        }
      ]
    },
    "source": {
      "type": "string",
      "enum": [
        "mcp",
        "ui",
        "system"
      ]
    },
    "summary": {
      "type": "string"
    },
    "reason": {
      "type": "string"
    },
    "correlation": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "runId": {
          "type": "string"
        },
        "requestId": {
          "type": "string"
        }
      }
    }
  },
  "required": [
    "projectId",
    "from",
    "to"
  ],
  "additionalProperties": false
}
```

</details>

#### `feedback_list`

Internal name: `feedback.list` · Effect: **read** · Autonomy: **safe**

List feedback records, newest first.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "nodeId": {
      "type": "string"
    },
    "runId": {
      "type": "string"
    },
    "kind": {
      "type": "string",
      "enum": [
        "approve",
        "reject",
        "edit",
        "outcome"
      ]
    },
    "limit": {
      "type": "integer",
      "minimum": 1,
      "maximum": 200
    }
  },
  "required": [],
  "additionalProperties": false
}
```

</details>

#### `feedback_record`

Internal name: `feedback.record` · Effect: **mutate-learning** · Autonomy: **caution**

Record human/analytics feedback: approve, reject, edit (with diff), or a published-performance outcome. Edit diffs are redacted before persistence.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "kind": {
      "type": "string",
      "enum": [
        "approve",
        "reject",
        "edit",
        "outcome"
      ]
    },
    "nodeId": {
      "type": "string"
    },
    "runId": {
      "type": "string"
    },
    "evalId": {
      "type": "string"
    },
    "editDiff": {
      "type": "object"
    },
    "outcome": {
      "type": "object"
    },
    "note": {
      "type": "string"
    },
    "expectedWorkspaceVersion": {
      "type": "integer",
      "minimum": 0
    },
    "baseRevisionId": {
      "type": "string"
    },
    "actor": {
      "oneOf": [
        {
          "type": "string",
          "minLength": 1
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "enum": [
                "human",
                "agent",
                "system"
              ]
            },
            "id": {
              "type": "string"
            },
            "label": {
              "type": "string"
            }
          }
        }
      ]
    },
    "source": {
      "type": "string",
      "enum": [
        "mcp",
        "ui",
        "system"
      ]
    },
    "summary": {
      "type": "string"
    },
    "reason": {
      "type": "string"
    },
    "correlation": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "runId": {
          "type": "string"
        },
        "requestId": {
          "type": "string"
        }
      }
    }
  },
  "required": [
    "kind"
  ],
  "additionalProperties": false
}
```

</details>

### `learning`

| Wire name | Effect | Storage written | External reach | Autonomy | Note |
|---|---|---|---|---|---|
| [`learning_archive_observation`](#learning_archive_observation) | mutate-learning | workspace/current.json | - | caution |  |
| [`learning_archive_observations`](#learning_archive_observations) | mutate-learning | workspace/current.json | - | caution |  |
| [`learning_list_observations`](#learning_list_observations) | read | - | - | safe | KNOWN DEFECT on gcs/blobs: lists the learning/ prefix, which the conversation-turn ledger shares (docs/KNOWN_ISSUES.md C-1). |
| [`learning_record_observation`](#learning_record_observation) | mutate-learning | workspace/current.json (learningObservations) | - | caution |  |

#### `learning_archive_observation`

Internal name: `learning.archive_observation` · Effect: **mutate-learning** · Autonomy: **caution**

Archive (soft-delete) one learning observation by id. The record is never removed — it gains status:"archived" plus archivedAt/archivedReason and is excluded from listObservations unless includeArchived is set.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "minLength": 1
    },
    "reason": {
      "type": "string",
      "minLength": 1,
      "description": "Optional human-readable reason recorded on the archived observation."
    }
  },
  "required": [
    "id"
  ],
  "additionalProperties": false
}
```

</details>

#### `learning_archive_observations`

Internal name: `learning.archive_observations` · Effect: **mutate-learning** · Autonomy: **caution**

Bulk-archive every ACTIVE observation whose text starts with textPrefix (e.g. a sunset coordination-board marker). Set dryRun:true to preview the count/ids without archiving anything.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "textPrefix": {
      "type": "string",
      "minLength": 1,
      "description": "Archive every active observation whose `observation` text starts with this prefix."
    },
    "reason": {
      "type": "string",
      "minLength": 1
    },
    "dryRun": {
      "type": "boolean",
      "description": "Preview the count/ids without archiving anything. Default false."
    }
  },
  "required": [
    "textPrefix"
  ],
  "additionalProperties": false
}
```

</details>

#### `learning_list_observations`

Internal name: `learning.list_observations` · Effect: **read** · Autonomy: **safe**

List learning observations. Archived (soft-deleted) observations are excluded by default.

> KNOWN DEFECT on gcs/blobs: lists the learning/ prefix, which the conversation-turn ledger shares (docs/KNOWN_ISSUES.md C-1).

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "includeArchived": {
      "type": "boolean",
      "description": "Include archived (soft-deleted) observations. Default false."
    }
  },
  "required": [],
  "additionalProperties": false
}
```

</details>

#### `learning_record_observation`

Internal name: `learning.record_observation` · Effect: **mutate-learning** · Autonomy: **caution**

Record a learning observation, optionally stamped with the runId/nodeId it came from.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "observation": {
      "type": "string",
      "minLength": 1
    },
    "metadata": {
      "type": "object"
    },
    "runId": {
      "type": "string",
      "minLength": 1,
      "description": "Optional: attribute this observation to the run that produced it, so it can be joined back later."
    },
    "nodeId": {
      "type": "string",
      "minLength": 1,
      "description": "Optional: attribute this observation to the node that produced it."
    }
  },
  "required": [
    "observation"
  ],
  "additionalProperties": false
}
```

</details>

### `node`

| Wire name | Effect | Storage written | External reach | Autonomy | Note |
|---|---|---|---|---|---|
| [`node_cancel`](#node_cancel) | mutate-run | runs/ | - | caution |  |
| [`node_execute`](#node_execute) | execute-model | runs/, usage/, artifacts/, workspace/current.json (stage mirror) | model provider; controlled tools may reach project MCP / web | caution | Standalone node execution (nodeRuntime.executeNode). Spends model budget. |
| [`node_get`](#node_get) | read | - | - | safe |  |
| [`node_get_effective_prompt`](#node_get_effective_prompt) | read | - | - | safe |  |
| [`node_get_effective_skills`](#node_get_effective_skills) | read | - | - | safe |  |
| [`node_get_effective_tools`](#node_get_effective_tools) | read | - | - | safe |  |
| [`node_get_input_schema`](#node_get_input_schema) | read | - | - | safe |  |
| [`node_get_latest_output`](#node_get_latest_output) | read | - | - | safe |  |
| [`node_get_output_schema`](#node_get_output_schema) | read | - | - | safe |  |
| [`node_list_executions`](#node_list_executions) | read | - | - | safe |  |
| [`node_list_outputs`](#node_list_outputs) | read | - | - | safe |  |
| [`node_prepare_execution`](#node_prepare_execution) | read | - | - | safe |  |
| [`node_retry`](#node_retry) | execute-model | runs/, usage/ | model provider | caution |  |
| [`node_validate_input`](#node_validate_input) | read | - | - | safe |  |
| [`node_validate_output`](#node_validate_output) | read | - | - | safe |  |

#### `node_cancel`

Internal name: `node.cancel` · Effect: **mutate-run** · Autonomy: **caution**

Cancel an independent node execution record.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "runId": {
      "type": "string",
      "minLength": 1
    },
    "nodeId": {
      "type": "string"
    },
    "executionId": {
      "type": "string"
    }
  },
  "required": [
    "runId"
  ],
  "additionalProperties": false
}
```

</details>

#### `node_execute`

Internal name: `node.execute` · Effect: **execute-model** · Autonomy: **caution**

Execute exactly one node independently from the full workflow.

> Standalone node execution (nodeRuntime.executeNode). Spends model budget.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "nodeId": {
      "type": "string",
      "minLength": 1
    },
    "input": {},
    "runId": {
      "type": "string"
    },
    "dependencyOutputs": {
      "type": "object"
    },
    "executionMode": {
      "type": "string",
      "enum": [
        "mock",
        "openai"
      ],
      "default": "openai",
      "description": "Execution mode. \"openai\" (DEFAULT) calls the configured model provider and produces real node output. \"mock\" produces deterministic placeholder output generated from each node's outputSchema — structurally valid but content-free, for cheap CI/test runs; mock artifacts must never be treated as publishable content. Every run reports its mode back on workflow.get_run / workflow.list_runs as `mode`."
    },
    "modelConfig": {
      "type": "object"
    },
    "expectedWorkspaceVersion": {
      "type": "integer",
      "minimum": 0
    }
  },
  "required": [
    "nodeId"
  ],
  "additionalProperties": false
}
```

</details>

#### `node_get`

Internal name: `node.get` · Effect: **read** · Autonomy: **safe**

Get a safe complete node inspection record with compact summaries of this node's actual revisions; use changes tools for full historical snapshots.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "nodeId": {
      "type": "string",
      "minLength": 1
    }
  },
  "required": [
    "nodeId"
  ],
  "additionalProperties": false
}
```

</details>

#### `node_get_effective_prompt`

Internal name: `node.get_effective_prompt` · Effect: **read** · Autonomy: **safe**

Resolve the effective prompt for one node without secrets.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "nodeId": {
      "type": "string",
      "minLength": 1
    }
  },
  "required": [
    "nodeId"
  ],
  "additionalProperties": false
}
```

</details>

#### `node_get_effective_skills`

Internal name: `node.get_effective_skills` · Effect: **read** · Autonomy: **safe**

Resolve effective skill policy for one node.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "nodeId": {
      "type": "string",
      "minLength": 1
    }
  },
  "required": [
    "nodeId"
  ],
  "additionalProperties": false
}
```

</details>

#### `node_get_effective_tools`

Internal name: `node.get_effective_tools` · Effect: **read** · Autonomy: **safe**

Resolve effective controlled tools for one node.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "nodeId": {
      "type": "string",
      "minLength": 1
    }
  },
  "required": [
    "nodeId"
  ],
  "additionalProperties": false
}
```

</details>

#### `node_get_input_schema`

Internal name: `node.get_input_schema` · Effect: **read** · Autonomy: **safe**

Get one node input schema.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "nodeId": {
      "type": "string",
      "minLength": 1
    }
  },
  "required": [
    "nodeId"
  ],
  "additionalProperties": false
}
```

</details>

#### `node_get_latest_output`

Internal name: `node.get_latest_output` · Effect: **read** · Autonomy: **safe**

Get latest node output with filters.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "nodeId": {
      "type": "string",
      "minLength": 1
    },
    "runId": {
      "type": "string"
    },
    "executionId": {
      "type": "string"
    },
    "artifactType": {
      "type": "string"
    },
    "from": {
      "type": "string",
      "format": "date-time"
    },
    "to": {
      "type": "string",
      "format": "date-time"
    }
  },
  "required": [],
  "additionalProperties": false
}
```

</details>

#### `node_get_output_schema`

Internal name: `node.get_output_schema` · Effect: **read** · Autonomy: **safe**

Get one node output schema.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "nodeId": {
      "type": "string",
      "minLength": 1
    }
  },
  "required": [
    "nodeId"
  ],
  "additionalProperties": false
}
```

</details>

#### `node_list_executions`

Internal name: `node.list_executions` · Effect: **read** · Autonomy: **safe**

List per-node execution records (status, timing, cost/tokens) by runId/nodeId/executionId — nodeId alone spans recent runs, runId alone lists every node in that run, both narrows to one.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "nodeId": {
      "type": "string",
      "minLength": 1
    },
    "runId": {
      "type": "string"
    },
    "executionId": {
      "type": "string"
    },
    "artifactType": {
      "type": "string"
    },
    "from": {
      "type": "string",
      "format": "date-time"
    },
    "to": {
      "type": "string",
      "format": "date-time"
    }
  },
  "required": [],
  "additionalProperties": false
}
```

</details>

#### `node_list_outputs`

Internal name: `node.list_outputs` · Effect: **read** · Autonomy: **safe**

List node outputs by node, run, execution, artifact type, or date range.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "nodeId": {
      "type": "string",
      "minLength": 1
    },
    "runId": {
      "type": "string"
    },
    "executionId": {
      "type": "string"
    },
    "artifactType": {
      "type": "string"
    },
    "from": {
      "type": "string",
      "format": "date-time"
    },
    "to": {
      "type": "string",
      "format": "date-time"
    }
  },
  "required": [],
  "additionalProperties": false
}
```

</details>

#### `node_prepare_execution`

Internal name: `node.prepare_execution` · Effect: **read** · Autonomy: **safe**

Prepare one node execution without calling the model.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "nodeId": {
      "type": "string",
      "minLength": 1
    },
    "input": {},
    "dependencyOutputs": {
      "type": "object"
    },
    "modelConfig": {
      "type": "object"
    }
  },
  "required": [
    "nodeId"
  ],
  "additionalProperties": false
}
```

</details>

#### `node_retry`

Internal name: `node.retry` · Effect: **execute-model** · Autonomy: **caution**

Retry a previous independent node execution.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "runId": {
      "type": "string",
      "minLength": 1
    },
    "nodeId": {
      "type": "string"
    },
    "executionId": {
      "type": "string"
    }
  },
  "required": [
    "runId"
  ],
  "additionalProperties": false
}
```

</details>

#### `node_validate_input`

Internal name: `node.validate_input` · Effect: **read** · Autonomy: **safe**

Validate input against a node input schema.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "nodeId": {
      "type": "string",
      "minLength": 1
    },
    "value": {}
  },
  "required": [
    "nodeId",
    "value"
  ],
  "additionalProperties": false
}
```

</details>

#### `node_validate_output`

Internal name: `node.validate_output` · Effect: **read** · Autonomy: **safe**

Validate output against a node output schema.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "nodeId": {
      "type": "string",
      "minLength": 1
    },
    "value": {}
  },
  "required": [
    "nodeId",
    "value"
  ],
  "additionalProperties": false
}
```

</details>

### `optimizer`

| Wire name | Effect | Storage written | External reach | Autonomy | Note |
|---|---|---|---|---|---|
| [`optimizer_analyze`](#optimizer_analyze) | read | - | - | safe |  |
| [`optimizer_auto_promote`](#optimizer_auto_promote) | mutate-workspace | workspace/current.json, changes/, revisions/ | - | operator | Eval-gated; low-risk nodes only; has a dry-run preview. |
| [`optimizer_promote`](#optimizer_promote) | mutate-workspace | workspace/current.json (node prompt), changes/, revisions/, improvement/proposals | - | operator | Changes a live prompt (store mode = next run). |
| [`optimizer_propose`](#optimizer_propose) | execute-model | improvement/proposals, usage/ | reflector model (mode openai) or none (mock) | caution | Propose-only; nothing is applied. |
| [`optimizer_run_trial`](#optimizer_run_trial) | execute-model | improvement/trials, runs/ (trial_ prefix), usage/ | model provider | caution |  |
| [`optimizer_status`](#optimizer_status) | read | - | - | safe |  |

#### `optimizer_analyze`

Internal name: `optimizer.analyze` · Effect: **read** · Autonomy: **safe**

Evidence-cited diagnosis of a node: eval score aggregates, worst criteria, run failure codes, and feedback counts.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "nodeId": {
      "type": "string",
      "minLength": 1
    },
    "from": {
      "type": "string",
      "format": "date-time"
    },
    "to": {
      "type": "string",
      "format": "date-time"
    }
  },
  "required": [
    "nodeId"
  ],
  "additionalProperties": false
}
```

</details>

#### `optimizer_auto_promote`

Internal name: `optimizer.auto_promote` · Effect: **mutate-workspace** · Autonomy: **operator**

Eval-gated automatic promotion (DIRECTION Phase 7): promote proposals whose champion/challenger TRIAL already proves the change is better, for LOW-RISK nodes only (publish/admin nodes are never auto-promoted). Only 'trialed' proposals qualify — a fresh 'proposed' draft has no trial evidence and is skipped. Set dryRun:true to preview eligibility without promoting. This is an explicit human trigger; the IMPROVEMENT_AUTO_PROMOTE flag governs the AUTOMATIC post-run path.

> Eval-gated; low-risk nodes only; has a dry-run preview.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "nodeId": {
      "type": "string"
    },
    "dryRun": {
      "type": "boolean",
      "description": "Preview eligible proposals without promoting."
    },
    "minScore": {
      "type": "number",
      "minimum": 0,
      "maximum": 1,
      "description": "Min trial meanChallengerScore to qualify (default 0.7)."
    },
    "max": {
      "type": "integer",
      "minimum": 1,
      "maximum": 50,
      "description": "Max promotions this pass (default 3)."
    },
    "expectedWorkspaceVersion": {
      "type": "integer",
      "minimum": 0
    },
    "baseRevisionId": {
      "type": "string"
    },
    "actor": {
      "oneOf": [
        {
          "type": "string",
          "minLength": 1
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "enum": [
                "human",
                "agent",
                "system"
              ]
            },
            "id": {
              "type": "string"
            },
            "label": {
              "type": "string"
            }
          }
        }
      ]
    },
    "source": {
      "type": "string",
      "enum": [
        "mcp",
        "ui",
        "system"
      ]
    },
    "summary": {
      "type": "string"
    },
    "reason": {
      "type": "string"
    },
    "correlation": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "runId": {
          "type": "string"
        },
        "requestId": {
          "type": "string"
        }
      }
    }
  },
  "required": [],
  "additionalProperties": false
}
```

</details>

#### `optimizer_promote`

Internal name: `optimizer.promote` · Effect: **mutate-workspace** · Autonomy: **operator**

Promote a proposal through the versioned change funnel (structured evidence-citing reason; rollback via changes.restore). Refuses stale proposals whose node prompt drifted since diagnosis.

> Changes a live prompt (store mode = next run).

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "proposalId": {
      "type": "string",
      "minLength": 1
    },
    "expectedWorkspaceVersion": {
      "type": "integer",
      "minimum": 0
    },
    "baseRevisionId": {
      "type": "string"
    },
    "actor": {
      "oneOf": [
        {
          "type": "string",
          "minLength": 1
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "enum": [
                "human",
                "agent",
                "system"
              ]
            },
            "id": {
              "type": "string"
            },
            "label": {
              "type": "string"
            }
          }
        }
      ]
    },
    "source": {
      "type": "string",
      "enum": [
        "mcp",
        "ui",
        "system"
      ]
    },
    "summary": {
      "type": "string"
    },
    "reason": {
      "type": "string"
    },
    "correlation": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "runId": {
          "type": "string"
        },
        "requestId": {
          "type": "string"
        }
      }
    }
  },
  "required": [
    "proposalId"
  ],
  "additionalProperties": false
}
```

</details>

#### `optimizer_propose`

Internal name: `optimizer.propose` · Effect: **execute-model** · Autonomy: **caution**

GEPA-style reflection: diagnose from eval evidence and propose a prompt mutation. PROPOSE-ONLY — nothing is applied until optimizer.promote.

> Propose-only; nothing is applied.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "nodeId": {
      "type": "string",
      "minLength": 1
    },
    "mode": {
      "type": "string",
      "enum": [
        "mock",
        "openai"
      ],
      "default": "mock"
    }
  },
  "required": [
    "nodeId"
  ],
  "additionalProperties": false
}
```

</details>

#### `optimizer_run_trial`

Internal name: `optimizer.run_trial` · Effect: **execute-model** · Autonomy: **caution**

Run a proposal (or an ad-hoc prompt/model variant) against a frozen replay dataset: each case re-executes with the variant, is rubric-scored, and is pairwise-judged against the champion output in BOTH orderings.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "proposalId": {
      "type": "string"
    },
    "nodeId": {
      "type": "string"
    },
    "promptVariant": {
      "type": "string"
    },
    "modelConfigVariant": {
      "type": "object"
    },
    "datasetId": {
      "type": "string"
    },
    "rubricId": {
      "type": "string"
    },
    "mode": {
      "type": "string",
      "enum": [
        "mock",
        "openai"
      ],
      "default": "mock"
    },
    "caseLimit": {
      "type": "integer",
      "minimum": 1,
      "maximum": 100
    }
  },
  "required": [],
  "additionalProperties": false
}
```

</details>

#### `optimizer_status`

Internal name: `optimizer.status` · Effect: **read** · Autonomy: **safe**

Proposals, trial summaries, and the cost-aware model-ladder recommendation for a node.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "nodeId": {
      "type": "string"
    }
  },
  "required": [],
  "additionalProperties": false
}
```

</details>

### `playbook`

| Wire name | Effect | Storage written | External reach | Autonomy | Note |
|---|---|---|---|---|---|
| [`playbook_apply_delta`](#playbook_apply_delta) | mutate-learning | improvement/playbooks | - | caution | Playbooks are injected into node prompts on the next dispatch. |
| [`playbook_curate`](#playbook_curate) | mutate-learning | improvement/playbooks, usage/ (openai mode) | curator model (mode openai) | caution |  |
| [`playbook_get`](#playbook_get) | read | - | - | safe |  |
| [`playbook_migrate_observations`](#playbook_migrate_observations) | mutate-learning | improvement/playbooks | - | caution |  |

#### `playbook_apply_delta`

Internal name: `playbook.apply_delta` · Effect: **mutate-learning** · Autonomy: **caution**

Apply a curated delta to a node's playbook: adds (deduplicated), helpful/harmful counters, retirements — budget-enforced.

> Playbooks are injected into node prompts on the next dispatch.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "nodeId": {
      "type": "string",
      "minLength": 1
    },
    "delta": {
      "type": "object"
    },
    "expectedWorkspaceVersion": {
      "type": "integer",
      "minimum": 0
    },
    "baseRevisionId": {
      "type": "string"
    },
    "actor": {
      "oneOf": [
        {
          "type": "string",
          "minLength": 1
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "enum": [
                "human",
                "agent",
                "system"
              ]
            },
            "id": {
              "type": "string"
            },
            "label": {
              "type": "string"
            }
          }
        }
      ]
    },
    "source": {
      "type": "string",
      "enum": [
        "mcp",
        "ui",
        "system"
      ]
    },
    "summary": {
      "type": "string"
    },
    "reason": {
      "type": "string"
    },
    "correlation": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "runId": {
          "type": "string"
        },
        "requestId": {
          "type": "string"
        }
      }
    }
  },
  "required": [
    "nodeId",
    "delta"
  ],
  "additionalProperties": false
}
```

</details>

#### `playbook_curate`

Internal name: `playbook.curate` · Effect: **mutate-learning** · Autonomy: **caution**

Reflector→Curator pass: derive a playbook delta from the node's evaluation evidence and apply it (dedup + item/char budget enforced). mode=mock (default) is the deterministic heuristic (weakest rubric criterion becomes a pitfall lesson); mode=openai runs the Curator LLM for richer adds (strategy/pitfall/constraint) plus retirement of stale lessons. A no-evidence node is a no-op.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "nodeId": {
      "type": "string",
      "minLength": 1
    },
    "mode": {
      "type": "string",
      "enum": [
        "mock",
        "openai"
      ],
      "default": "mock"
    }
  },
  "required": [
    "nodeId"
  ],
  "additionalProperties": false
}
```

</details>

#### `playbook_get`

Internal name: `playbook.get` · Effect: **read** · Autonomy: **safe**

Get a node's ACE playbook (curated, budgeted lessons) and its rendered prompt-injection form.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "nodeId": {
      "type": "string",
      "minLength": 1
    }
  },
  "required": [
    "nodeId"
  ],
  "additionalProperties": false
}
```

</details>

#### `playbook_migrate_observations`

Internal name: `playbook.migrate_observations` · Effect: **mutate-learning** · Autonomy: **caution**

One-shot curation of legacy global learning observations into per-node playbooks (observations with a nodeId, top-level or in metadata). Archived observations are excluded. The learning.* tools stay untouched.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "dryRun": {
      "type": "boolean"
    }
  },
  "required": [],
  "additionalProperties": false
}
```

</details>

### `project`

| Wire name | Effect | Storage written | External reach | Autonomy | Note |
|---|---|---|---|---|---|
| [`project_call_read_tool`](#project_call_read_tool) | external-read | - | READ_TOOL_ALLOWLIST operations on the project MCP | safe |  |
| [`project_call_tool`](#project_call_tool) | external-mutate | - | ANY allowed tool on the project MCP (incl. publish/deploy/commerce for full-access projects) | operator | Permission = toolPolicies > allowedTools > defaultToolPolicy > blocked. Dr. Lurie defaults to allowed for everything except wipe_blob_stores. |
| [`project_create`](#project_create) | mutate-registry | projects/ | - | caution |  |
| [`project_delete`](#project_delete) | mutate-registry | projects/ | - | operator |  |
| [`project_get`](#project_get) | read | - | - | safe |  |
| [`project_get_registration_contract`](#project_get_registration_contract) | read | - | - | safe |  |
| [`project_list`](#project_list) | read | - | - | safe |  |
| [`project_list_tools`](#project_list_tools) | external-read | - | project MCP tools/list | safe |  |
| [`project_test_connection`](#project_test_connection) | external-read | - | project MCP initialize | safe |  |
| [`project_update`](#project_update) | mutate-registry | projects/ | - | operator | Can widen tool policies and set autonomyMode; no version check on save. |
| [`project_validate_handoff`](#project_validate_handoff) | read | - | - | safe |  |

#### `project_call_read_tool`

Internal name: `project.call_read_tool` · Effect: **external-read** · Autonomy: **safe**

Call a read-only tool on a registered project MCP server, without approval. Permitted operations are a fixed, server-side allowlist (object_contract, registry_get, object_inventory, object_get, object_list, object_validate, ping, list_sources, list_connections, search_offers, performance, demand_signals, explain_decision, list_pdf_templates, get_image_model_policy) — never caller-supplied; anything else is refused before any transport with code "read_tool_operation_not_permitted". Still honors the project's own toolPolicies/defaultToolPolicy and the executable project policy (legacy artifact fallback blocks) — a project can still block a read op. Use project.call_tool for writes.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "projectId": {
      "type": "string",
      "minLength": 1
    },
    "tool": {
      "type": "string",
      "minLength": 1
    },
    "arguments": {
      "type": "object",
      "additionalProperties": true
    }
  },
  "required": [
    "projectId",
    "tool",
    "arguments"
  ],
  "additionalProperties": false
}
```

</details>

#### `project_call_tool`

Internal name: `project.call_tool` · Effect: **external-mutate** · Autonomy: **operator**

Call an approved tool on a registered project MCP server. The config permission model plus the project's executable policy apply: legacy artifact fallback tools and fallback artifact-source arguments (remote image URLs, copied artifact refs, repo paths, hand-authored blob keys) are blocked before any transport, even when the config marks the tool allowed.

> Permission = toolPolicies > allowedTools > defaultToolPolicy > blocked. Dr. Lurie defaults to allowed for everything except wipe_blob_stores.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "projectId": {
      "type": "string",
      "minLength": 1
    },
    "tool": {
      "type": "string",
      "minLength": 1
    },
    "arguments": {
      "type": "object",
      "additionalProperties": true
    }
  },
  "required": [
    "projectId",
    "tool",
    "arguments"
  ],
  "additionalProperties": false
}
```

</details>

#### `project_create`

Internal name: `project.create` · Effect: **mutate-registry** · Autonomy: **caution**

Register a new external publishing-client MCP connection. The TOKEN is referenced by environment variable NAME only (never a value). The ENDPOINT can be passed directly as mcpEndpoint and is stored on the record — an endpoint URL is not a secret — so registering a tenant needs no new env var on this deployment; <CLIENT>_MCP_ENDPOINT still overrides it when set. Publishing stays governed by server-side policy. A bearer_env project is created with no clientSiteBinding (that field is only settable via project.update) — the result carries an advisories[] entry when authMode is bearer_env, since bearer_env also covers internal service projects (monetizer, pdf-tool) that never get one; ignore the advisory for those.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "project": {
      "type": "object",
      "properties": {
        "projectId": {
          "type": "string",
          "pattern": "^[a-z0-9][a-z0-9-]{1,62}$",
          "description": "Lowercase kebab-case id, e.g. acme-daily."
        },
        "name": {
          "type": "string",
          "minLength": 1,
          "maxLength": 120
        },
        "mcpEndpointEnvVar": {
          "type": "string",
          "pattern": "^[A-Z][A-Z0-9_]{2,63}$",
          "description": "Environment variable NAME for the MCP endpoint URL — never the URL itself. Required, and it still WINS when populated (the break-glass override), but you no longer have to SET it in the deployment if you pass mcpEndpoint."
        },
        "mcpEndpoint": {
          "type": "string",
          "format": "uri",
          "maxLength": 512,
          "description": "The MCP endpoint URL ITSELF, stored on the registry record — an endpoint is not a secret (the TOKEN is, and stays an env var NAME). https only, no user:password@, no query, no fragment, so a credential cannot be smuggled into the registry. Supplying it means a new tenant needs no <CLIENT>_MCP_ENDPOINT env var on this deployment. Resolution: env var first, this second. On project.update, null clears it."
        },
        "authMode": {
          "type": "string",
          "enum": [
            "none",
            "bearer_env"
          ],
          "default": "bearer_env"
        },
        "tokenEnvVar": {
          "type": "string",
          "pattern": "^[A-Z][A-Z0-9_]{2,63}$",
          "description": "Environment variable NAME holding the bearer token — never the token itself. Required for bearer_env."
        },
        "tokenSecretRef": {
          "type": "string",
          "pattern": "^projects/[a-z][a-z0-9-]{4,28}[a-z0-9]/secrets/[A-Za-z0-9_-]{1,255}/versions/(?:latest|[1-9][0-9]{0,18})$",
          "maxLength": 512,
          "description": "Secret Manager VERSION RESOURCE NAME holding the bearer token — a pointer, never the value (e.g. projects/my-project/secrets/tenant-acme-mcp-token/versions/latest). PREFERRED over tokenEnvVar: resolution is env var first, this second, and a plane that carries no tenant environment at all (the continuation-tick Cloud Run job) still resolves it with its own service-account identity, so a new tenant needs no deployment change on any plane. Dereferencing requires roles/secretmanager.secretAccessor on each executing plane's identity. On project.update, null clears it."
        },
        "allowedTools": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [],
          "description": "Legacy allow-list; a listed tool resolves to \"allowed\". toolPolicies/defaultToolPolicy are the richer control."
        },
        "defaultToolPolicy": {
          "type": "string",
          "enum": [
            "allowed",
            "needs_approval",
            "blocked"
          ],
          "description": "Fallback permission for any tool not named in allowedTools/toolPolicies. Absent = blocked (deny-all)."
        },
        "toolPolicies": {
          "type": "object",
          "additionalProperties": {
            "type": "string",
            "enum": [
              "allowed",
              "needs_approval",
              "blocked"
            ]
          },
          "description": "Per-tool permission overrides (highest precedence): allowed | needs_approval | blocked."
        },
        "contentContract": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "contentContract": {
              "type": "string"
            }
          }
        },
        "status": {
          "type": "string",
          "enum": [
            "active",
            "disabled"
          ],
          "default": "active"
        }
      },
      "required": [
        "projectId",
        "name",
        "mcpEndpointEnvVar"
      ],
      "additionalProperties": false
    },
    "expectedWorkspaceVersion": {
      "type": "integer",
      "minimum": 0
    },
    "baseRevisionId": {
      "type": "string"
    },
    "actor": {
      "oneOf": [
        {
          "type": "string",
          "minLength": 1
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "enum": [
                "human",
                "agent",
                "system"
              ]
            },
            "id": {
              "type": "string"
            },
            "label": {
              "type": "string"
            }
          }
        }
      ]
    },
    "source": {
      "type": "string",
      "enum": [
        "mcp",
        "ui",
        "system"
      ]
    },
    "summary": {
      "type": "string"
    },
    "reason": {
      "type": "string"
    },
    "correlation": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "runId": {
          "type": "string"
        },
        "requestId": {
          "type": "string"
        }
      }
    }
  },
  "required": [
    "project"
  ],
  "additionalProperties": false
}
```

</details>

#### `project_delete`

Internal name: `project.delete` · Effect: **mutate-registry** · Autonomy: **operator**

Remove an agent-registered project connection. Code-defined default projects cannot be deleted (set status to disabled instead).

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "projectId": {
      "type": "string",
      "minLength": 1
    },
    "expectedWorkspaceVersion": {
      "type": "integer",
      "minimum": 0
    },
    "baseRevisionId": {
      "type": "string"
    },
    "actor": {
      "oneOf": [
        {
          "type": "string",
          "minLength": 1
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "enum": [
                "human",
                "agent",
                "system"
              ]
            },
            "id": {
              "type": "string"
            },
            "label": {
              "type": "string"
            }
          }
        }
      ]
    },
    "source": {
      "type": "string",
      "enum": [
        "mcp",
        "ui",
        "system"
      ]
    },
    "summary": {
      "type": "string"
    },
    "reason": {
      "type": "string"
    },
    "correlation": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "runId": {
          "type": "string"
        },
        "requestId": {
          "type": "string"
        }
      }
    }
  },
  "required": [
    "projectId"
  ],
  "additionalProperties": false
}
```

</details>

#### `project_get`

Internal name: `project.get` · Effect: **read** · Autonomy: **safe**

Get one registered project MCP connection with safe, non-secret metadata, plus the project's knowledge rules when a hook module provides them.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "projectId": {
      "type": "string",
      "minLength": 1
    }
  },
  "required": [
    "projectId"
  ],
  "additionalProperties": false
}
```

</details>

#### `project_get_registration_contract`

Internal name: `project.get_registration_contract` · Effect: **read** · Autonomy: **safe**

Machine-readable contract for onboarding a new publishing client: field rules, env-var naming conventions, and the step-by-step registration flow.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {},
  "required": [],
  "additionalProperties": false
}
```

</details>

#### `project_list`

Internal name: `project.list` · Effect: **read** · Autonomy: **safe**

List registered project MCP connections with safe, non-secret metadata.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {},
  "required": [],
  "additionalProperties": false
}
```

</details>

#### `project_list_tools`

Internal name: `project.list_tools` · Effect: **external-read** · Autonomy: **safe**

List a project's remote MCP tools via tools/list. Returns safe tool names and descriptions only.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "projectId": {
      "type": "string",
      "minLength": 1
    }
  },
  "required": [
    "projectId"
  ],
  "additionalProperties": false
}
```

</details>

#### `project_test_connection`

Internal name: `project.test_connection` · Effect: **external-read** · Autonomy: **safe**

Run a primitive MCP initialize against a project's external server. Read-only; no publishing side effects.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "projectId": {
      "type": "string",
      "minLength": 1
    }
  },
  "required": [
    "projectId"
  ],
  "additionalProperties": false
}
```

</details>

#### `project_update`

Internal name: `project.update` · Effect: **mutate-registry** · Autonomy: **operator**

Patch a registered project's safe fields (name, env var names, the stored mcpEndpoint — null clears it, auth mode, allowed tools, contract, status, the client-site binding used by the fleet credential reconciler — null clears it) plus one policy field: autonomyMode (autonomous | operator-gated) — whether a run with no recorded operator decision proceeds under policy authority (publishingPolicy.autonomyMode). Identity and the REST of publishing policy (publishEnabled, requiresExplicitPublish) are not patchable.

> Can widen tool policies and set autonomyMode; no version check on save.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "projectId": {
      "type": "string",
      "minLength": 1
    },
    "patch": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "minLength": 1,
          "maxLength": 120
        },
        "mcpEndpointEnvVar": {
          "type": "string",
          "pattern": "^[A-Z][A-Z0-9_]{2,63}$",
          "description": "Environment variable NAME for the MCP endpoint URL — never the URL itself. Required, and it still WINS when populated (the break-glass override), but you no longer have to SET it in the deployment if you pass mcpEndpoint."
        },
        "mcpEndpoint": {
          "oneOf": [
            {
              "type": "string",
              "format": "uri",
              "maxLength": 512
            },
            {
              "type": "null"
            }
          ],
          "description": "The MCP endpoint URL stored on the record (https, no credentials/query/fragment); null clears it, returning the project to env-var-only endpoint resolution."
        },
        "authMode": {
          "type": "string",
          "enum": [
            "none",
            "bearer_env"
          ],
          "default": "bearer_env"
        },
        "tokenEnvVar": {
          "oneOf": [
            {
              "type": "string",
              "pattern": "^[A-Z][A-Z0-9_]{2,63}$"
            },
            {
              "type": "null"
            }
          ],
          "description": "Env var NAME for the bearer token; null removes it (only valid when authMode is none)."
        },
        "tokenSecretRef": {
          "oneOf": [
            {
              "type": "string",
              "pattern": "^projects/[a-z][a-z0-9-]{4,28}[a-z0-9]/secrets/[A-Za-z0-9_-]{1,255}/versions/(?:latest|[1-9][0-9]{0,18})$",
              "maxLength": 512
            },
            {
              "type": "null"
            }
          ],
          "description": "Secret Manager VERSION RESOURCE NAME for the bearer token (a pointer, never the value); null clears it, returning the project to env-var-only token resolution. Backfilling this on an existing project is what lets an executor plane with no tenant environment resolve its token."
        },
        "allowedTools": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [],
          "description": "Legacy allow-list; a listed tool resolves to \"allowed\". toolPolicies/defaultToolPolicy are the richer control."
        },
        "defaultToolPolicy": {
          "type": "string",
          "enum": [
            "allowed",
            "needs_approval",
            "blocked"
          ],
          "description": "Fallback permission for any tool not named in allowedTools/toolPolicies. Absent = blocked (deny-all)."
        },
        "toolPolicies": {
          "type": "object",
          "additionalProperties": {
            "type": "string",
            "enum": [
              "allowed",
              "needs_approval",
              "blocked"
            ]
          },
          "description": "Per-tool permission overrides (highest precedence): allowed | needs_approval | blocked."
        },
        "contentContract": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "contentContract": {
              "type": "string"
            }
          }
        },
        "status": {
          "type": "string",
          "enum": [
            "active",
            "disabled"
          ],
          "default": "active"
        },
        "clientSiteBinding": {
          "oneOf": [
            {
              "type": "object",
              "additionalProperties": false,
              "properties": {
                "netlifySiteName": {
                  "type": "string",
                  "minLength": 1,
                  "maxLength": 256
                },
                "netlifySiteId": {
                  "type": "string",
                  "minLength": 1,
                  "maxLength": 256
                }
              },
              "required": [
                "netlifySiteName"
              ]
            },
            {
              "type": "null"
            }
          ],
          "description": "Durable, non-secret client-site identity (Netlify site name, and optionally its site id) that makes this project eligible for the fleet Client Manager credential reconciler. null clears it. Set this on a legacy client-site project the reconciler otherwise cannot see at all."
        },
        "autonomyMode": {
          "type": "string",
          "enum": [
            "autonomous",
            "operator-gated"
          ],
          "description": "Whether a NEW run for this project proceeds under policy authority when no operator decision is recorded (publishingPolicy.autonomyMode). \"operator-gated\" (or omitting this field entirely) is today's unchanged behavior: no run publishes without an explicit operator approval. \"autonomous\" never stamps run.operatorPublishDecision — authority is resolved at gate-evaluation time (publishDecision.resolvePublishAuthority) — and an explicit \"withheld\", set via workflow.set_operator_publish_decision, always overrides it."
        }
      },
      "required": [],
      "additionalProperties": false
    },
    "expectedWorkspaceVersion": {
      "type": "integer",
      "minimum": 0
    },
    "baseRevisionId": {
      "type": "string"
    },
    "actor": {
      "oneOf": [
        {
          "type": "string",
          "minLength": 1
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "enum": [
                "human",
                "agent",
                "system"
              ]
            },
            "id": {
              "type": "string"
            },
            "label": {
              "type": "string"
            }
          }
        }
      ]
    },
    "source": {
      "type": "string",
      "enum": [
        "mcp",
        "ui",
        "system"
      ]
    },
    "summary": {
      "type": "string"
    },
    "reason": {
      "type": "string"
    },
    "correlation": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "runId": {
          "type": "string"
        },
        "requestId": {
          "type": "string"
        }
      }
    }
  },
  "required": [
    "projectId",
    "patch"
  ],
  "additionalProperties": false
}
```

</details>

#### `project_validate_handoff`

Internal name: `project.validate_handoff` · Effect: **read** · Autonomy: **safe**

Dry structural validation of a handoff against the project content_source.v1 / client_object.v1 contract. Read-only; no publishing.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "projectId": {
      "type": "string",
      "minLength": 1
    },
    "contentSource": {},
    "articleBody": {}
  },
  "required": [
    "projectId"
  ],
  "additionalProperties": false
}
```

</details>

### `publish`

| Wire name | Effect | Storage written | External reach | Autonomy | Note |
|---|---|---|---|---|---|
| [`publish_build_payload`](#publish_build_payload) | external-read | - | one read-only object_validate on the project MCP (runId form) | safe | Never publishes. |
| [`publish_validate_payload`](#publish_validate_payload) | read | - | - | safe |  |

#### `publish_build_payload`

Internal name: `publish.build_payload` · Effect: **external-read** · Autonomy: **safe**

Build a dry-run publish payload without side effects. With `articleBody`: wraps a body you already hold in a {target, dryRun, builtAt} envelope; the body must satisfy the article_body node's own outputSchema (see node.get_output_schema) and an invalid body is refused with the failing fields named. With `runId`: projects the dry_run_publish_payload.v1 that publish_payload would emit for that run, built by the same deterministic engine the executor uses (client object carried by reference from the run's article_body output, one read-only object_validate against the client, blockers = union(upstream) - resolved). Never publishes, patches or releases.

> Never publishes.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "articleBody": {
      "type": "object",
      "description": "Client-shaped client_object.v1 envelope (formerly article_body.v1) produced by the article_body node. Validated against that node's own outputSchema (see node.get_output_schema), never a workspace-local article schema."
    },
    "runId": {
      "type": "string",
      "minLength": 1,
      "description": "Project what publish_payload would emit for this run, built deterministically from the run's own article_body/artifact_plan stage outputs (dry_run_publish_payload.v1). Mutually exclusive with articleBody."
    },
    "target": {
      "type": "string",
      "enum": [
        "preview",
        "cms"
      ],
      "default": "preview"
    }
  },
  "required": [],
  "additionalProperties": false
}
```

</details>

#### `publish_validate_payload`

Internal name: `publish.validate_payload` · Effect: **read** · Autonomy: **safe**

Validate a dry-run publish payload: envelope fields (target, dryRun, builtAt) plus the articleBody against the article_body node's own outputSchema.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "payload": {
      "type": "object",
      "properties": {
        "articleBody": {
          "type": "object",
          "description": "Client-shaped client_object.v1 envelope (formerly article_body.v1) produced by the article_body node. Validated against that node's own outputSchema (see node.get_output_schema), never a workspace-local article schema."
        },
        "target": {
          "type": "string",
          "enum": [
            "preview",
            "cms"
          ]
        },
        "dryRun": {
          "const": true
        },
        "builtAt": {
          "type": "string",
          "format": "date-time"
        }
      },
      "required": [
        "articleBody",
        "target",
        "dryRun",
        "builtAt"
      ],
      "additionalProperties": false
    }
  },
  "required": [
    "payload"
  ],
  "additionalProperties": false
}
```

</details>

### `repository`

| Wire name | Effect | Storage written | External reach | Autonomy | Note |
|---|---|---|---|---|---|
| [`repository_get_health`](#repository_get_health) | read | - | - | safe |  |

#### `repository_get_health`

Internal name: `repository.get_health` · Effect: **read** · Autonomy: **safe**

Return safe repository health metadata.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {},
  "required": [],
  "additionalProperties": false
}
```

</details>

### `site`

| Wire name | Effect | Storage written | External reach | Autonomy | Note |
|---|---|---|---|---|---|
| [`site_duplicate`](#site_duplicate) | admin | projects/, runs/, auth/managed-scoped-bearers.v1.json, run-index/ | Netlify API (site create, env vars, build hook, deploy), platform create-site CLI, project MCP; starts a capture/clone run | operator | Genesis. SITE_GENESIS_NETLIFY_MODE=live performs real writes. |
| [`site_duplicate_status`](#site_duplicate_status) | read | - | - | safe |  |

#### `site_duplicate`

Internal name: `site.duplicate` · Effect: **admin** · Autonomy: **operator**

ONE CALL: duplicate a source site into a landing tenant with the capture_conductor workflow (crawl → map → theme → emit never-released drafts → score → report). With targetProjectId: verifies the existing project is reachable and capture-authorized (registry capturePolicy — the deny-all default refuses). With newSite: runs genesis first: create-site scaffold via the platform seam; Netlify site, build hook and deterministic env under NETLIFY_API_TOKEN; project.create with the tenant MCP endpoint derived from the site; and the Platform-site → Client Manager credential minted internally, persisted only as a digest/policy, installed as secret/function-only Netlify env, verified, and discarded without a human ever handling its value. Remaining account-authority steps are surfaced in the human checklist, never silently skipped. Starts the run AND kicks the long-run plane in the same call. Returns {runId, statusTool, humanChecklist}. Publish/release stay unreachable from every capture node; no secret VALUE ever transits this tool.

> Genesis. SITE_GENESIS_NETLIFY_MODE=live performs real writes.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "sourceUrl": {
      "type": "string",
      "format": "uri",
      "description": "HTTPS source to duplicate. Must fall inside the target project's capturePolicy (allowed origins/path prefixes); an out-of-policy source is refused before any run is created."
    },
    "targetProjectId": {
      "type": "string",
      "minLength": 1,
      "description": "Existing registered project to land the duplication in. Verified reachable (MCP initialize) and capture-authorized (registry capturePolicy; the deny-all default refuses). Mutually exclusive with newSite."
    },
    "newSite": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "minLength": 2,
          "description": "Lowercase kebab-case slug for the new tenant (repo tree sites/<name>/, registry projectId, <NAME>_MCP_* env var names)."
        },
        "netlifySiteName": {
          "type": "string",
          "minLength": 2,
          "description": "Optional Netlify site name (the <name> in <name>.netlify.app) when it must differ from the slug."
        },
        "mcpEndpoint": {
          "type": "string",
          "format": "uri",
          "maxLength": 512,
          "description": "Optional override for the new tenant's MCP endpoint, stored on its registry record (https, no credentials/query/fragment — an endpoint is not a secret, the token still is). OMIT IT normally: genesis derives the endpoint from the Netlify site it just creates, so no endpoint has to be set by hand anywhere. Use it only when the tenant serves /mcp from a custom domain from day one."
        }
      },
      "required": [
        "name"
      ],
      "additionalProperties": false
    },
    "budgetUsd": {
      "type": "number",
      "minimum": 0,
      "description": "Optional per-run cost ceiling in USD (workflow.start_dry_run semantics); defaults to $5 to ensure every autonomous duplication runs under an explicit ceiling. Refused as budget_exceeded when below the workflow's entry-node reservation — such a run could never dispatch its first node."
    },
    "executionMode": {
      "type": "string",
      "enum": [
        "mock",
        "openai"
      ],
      "default": "openai",
      "description": "Passed through to the run. \"mock\" is the cheap CI/test mode; deterministic capture stages run real engine code either way."
    }
  },
  "required": [
    "sourceUrl"
  ],
  "additionalProperties": false
}
```

</details>

#### `site_duplicate_status`

Internal name: `site.duplicate_status` · Effect: **read** · Autonomy: **safe**

Observe a site.duplicate run: run state (+ stall assessment), per-node progress, spend (cost ledger + budget), the capture report references (emission plan, drafts, fidelity, gaps, terminal run report), and the outstanding human checklist items — with the deploy-side item resolved live against the project's resolved connection (env var name + which source answers the endpoint; never a value). Read-only.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "runId": {
      "type": "string",
      "minLength": 1
    }
  },
  "required": [
    "runId"
  ],
  "additionalProperties": false
}
```

</details>

### `site_credentials_apply`

| Wire name | Effect | Storage written | External reach | Autonomy | Note |
|---|---|---|---|---|---|
| [`site_credentials_apply`](#site_credentials_apply) | admin | - | Cloud Run Jobs API (fires site-credential-reconciler --apply) | operator | Requires SITE_CREDENTIAL_RECONCILER_GCP_PROJECT/REGION on the service. |

#### `site_credentials_apply`

Internal name: `site_credentials_apply` · Effect: **admin** · Autonomy: **operator**

Fire the site-credential-reconciler Cloud Run Job with --apply and return immediately with its execution name. Never blocks: a live apply mints and installs credentials and waits out a production Netlify deploy per rotated tenant, which is minutes of work this MCP call cannot hold open. Poll progress with site_credentials_execution_status. Operator tool — not on any tenant's scoped chat allowlist.

> Requires SITE_CREDENTIAL_RECONCILER_GCP_PROJECT/REGION on the service.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {},
  "required": [],
  "additionalProperties": false
}
```

</details>

### `site_credentials_execution_status`

| Wire name | Effect | Storage written | External reach | Autonomy | Note |
|---|---|---|---|---|---|
| [`site_credentials_execution_status`](#site_credentials_execution_status) | external-read | - | Cloud Run Jobs API | safe |  |

#### `site_credentials_execution_status`

Internal name: `site_credentials_execution_status` · Effect: **external-read** · Autonomy: **safe**

Poll one site_credentials_apply execution: state (succeeded/failed/running/pending), start/completion timestamps, and per-task succeeded/failed counts. Read-only. Operator tool — not on any tenant's scoped chat allowlist.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "executionName": {
      "type": "string",
      "minLength": 1,
      "description": "Full Cloud Run execution resource name returned by site_credentials_apply, e.g. projects/p/locations/r/jobs/site-credential-reconciler/executions/site-credential-reconciler-abcde."
    }
  },
  "required": [
    "executionName"
  ],
  "additionalProperties": false
}
```

</details>

### `site_credentials_plan`

| Wire name | Effect | Storage written | External reach | Autonomy | Note |
|---|---|---|---|---|---|
| [`site_credentials_plan`](#site_credentials_plan) | read | - | - | safe | Internal name has no dot: MCP_EXPOSED_TOOL_PREFIXES=site does NOT expose it. |

#### `site_credentials_plan`

Internal name: `site_credentials_plan` · Effect: **read** · Autonomy: **safe**

Read-only dry run of the fleet Client Manager credential reconciler: for every bearer_env project, reports whether its scoped chat bearer is current, would be rotated on an apply, or is unmanaged (no client-site binding, so the reconciler cannot act on it at all — see unmanagedCount). No Netlify or Cloud Run call — same in-process check reconcileSiteCredentialsMain.ts runs without --apply.

> Internal name has no dot: MCP_EXPOSED_TOOL_PREFIXES=site does NOT expose it.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {},
  "required": [],
  "additionalProperties": false
}
```

</details>

### `skill`

| Wire name | Effect | Storage written | External reach | Autonomy | Note |
|---|---|---|---|---|---|
| [`skill_assign`](#skill_assign) | mutate-workspace | workspace/current.json (node.assignedSkills), changes/, revisions/ | - | caution |  |
| [`skill_clone`](#skill_clone) | mutate-workspace | skills/* | - | caution |  |
| [`skill_create`](#skill_create) | mutate-workspace | skills/current, skills/versions, skills/events | - | caution |  |
| [`skill_delete`](#skill_delete) | mutate-workspace | skills/* | - | caution |  |
| [`skill_get`](#skill_get) | read | - | - | safe |  |
| [`skill_get_version`](#skill_get_version) | read | - | - | safe |  |
| [`skill_list`](#skill_list) | read | - | - | safe |  |
| [`skill_list_versions`](#skill_list_versions) | read | - | - | safe |  |
| [`skill_resolve_for_node`](#skill_resolve_for_node) | read | - | - | safe |  |
| [`skill_restore_version`](#skill_restore_version) | mutate-workspace | skills/* | - | caution |  |
| [`skill_unassign`](#skill_unassign) | mutate-workspace | workspace/current.json, changes/, revisions/ | - | caution |  |
| [`skill_update`](#skill_update) | mutate-workspace | skills/* | - | caution |  |
| [`skill_validate`](#skill_validate) | read | - | - | safe |  |

#### `skill_assign`

Internal name: `skill.assign` · Effect: **mutate-workspace** · Autonomy: **caution**

Assign a skill id to a node without copying skill text into the node.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "skillId": {
      "type": "string",
      "minLength": 1
    },
    "newSkillId": {
      "type": "string",
      "minLength": 1
    },
    "nodeId": {
      "type": "string",
      "minLength": 1
    },
    "versionId": {
      "type": "string",
      "minLength": 1
    },
    "skill": {},
    "patch": {
      "type": "object"
    },
    "workspaceSystemPolicy": {
      "type": "string"
    },
    "projectPolicy": {
      "type": "string"
    },
    "runInstructions": {
      "type": "string"
    },
    "platformTools": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "runAuthorizedTools": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "riskPolicy": {
      "type": "string",
      "enum": [
        "read",
        "write",
        "publish",
        "admin"
      ]
    },
    "expectedWorkspaceVersion": {
      "type": "integer",
      "minimum": 0
    },
    "baseRevisionId": {
      "type": "string"
    },
    "actor": {
      "oneOf": [
        {
          "type": "string",
          "minLength": 1
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "enum": [
                "human",
                "agent",
                "system"
              ]
            },
            "id": {
              "type": "string"
            },
            "label": {
              "type": "string"
            }
          }
        }
      ]
    },
    "source": {
      "type": "string",
      "enum": [
        "mcp",
        "ui",
        "system"
      ]
    },
    "summary": {
      "type": "string"
    },
    "reason": {
      "type": "string"
    },
    "correlation": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "runId": {
          "type": "string"
        },
        "requestId": {
          "type": "string"
        }
      }
    }
  },
  "required": [],
  "additionalProperties": false
}
```

</details>

#### `skill_clone`

Internal name: `skill.clone` · Effect: **mutate-workspace** · Autonomy: **caution**

Clone a reusable skill under a new id.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "skillId": {
      "type": "string",
      "minLength": 1
    },
    "newSkillId": {
      "type": "string",
      "minLength": 1
    },
    "nodeId": {
      "type": "string",
      "minLength": 1
    },
    "versionId": {
      "type": "string",
      "minLength": 1
    },
    "skill": {},
    "patch": {
      "type": "object"
    },
    "workspaceSystemPolicy": {
      "type": "string"
    },
    "projectPolicy": {
      "type": "string"
    },
    "runInstructions": {
      "type": "string"
    },
    "platformTools": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "runAuthorizedTools": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "riskPolicy": {
      "type": "string",
      "enum": [
        "read",
        "write",
        "publish",
        "admin"
      ]
    },
    "expectedWorkspaceVersion": {
      "type": "integer",
      "minimum": 0
    },
    "baseRevisionId": {
      "type": "string"
    },
    "actor": {
      "oneOf": [
        {
          "type": "string",
          "minLength": 1
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "enum": [
                "human",
                "agent",
                "system"
              ]
            },
            "id": {
              "type": "string"
            },
            "label": {
              "type": "string"
            }
          }
        }
      ]
    },
    "source": {
      "type": "string",
      "enum": [
        "mcp",
        "ui",
        "system"
      ]
    },
    "summary": {
      "type": "string"
    },
    "reason": {
      "type": "string"
    },
    "correlation": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "runId": {
          "type": "string"
        },
        "requestId": {
          "type": "string"
        }
      }
    }
  },
  "required": [],
  "additionalProperties": false
}
```

</details>

#### `skill_create`

Internal name: `skill.create` · Effect: **mutate-workspace** · Autonomy: **caution**

Create a versioned reusable skill from a nested `skill` object; only skillId/name/description/instructions are required, other fields are defaulted.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "skill": {
      "type": "object",
      "properties": {
        "skillId": {
          "type": "string",
          "minLength": 1,
          "description": "Stable unique skill id, e.g. my_skill."
        },
        "name": {
          "type": "string",
          "minLength": 1
        },
        "description": {
          "type": "string",
          "minLength": 1
        },
        "instructions": {
          "type": "string",
          "minLength": 1,
          "description": "What a node does when it runs this skill."
        },
        "version": {
          "type": "string",
          "minLength": 1,
          "description": "Defaults to 1.0.0."
        },
        "status": {
          "type": "string",
          "enum": [
            "draft",
            "active",
            "deprecated"
          ],
          "description": "Defaults to active."
        },
        "riskLevel": {
          "type": "string",
          "enum": [
            "read",
            "write",
            "publish",
            "admin"
          ],
          "description": "Defaults to read."
        },
        "inputSchema": {
          "description": "JSON Schema for the skill input; defaults to { type: object }."
        },
        "outputSchema": {
          "description": "JSON Schema for the skill output; defaults to { type: object }."
        },
        "allowedTools": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "requiredArtifacts": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "producedArtifacts": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "examples": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "name": {
                "type": "string",
                "minLength": 1
              },
              "input": {},
              "output": {},
              "notes": {
                "type": "string"
              }
            },
            "required": [
              "name",
              "input",
              "output"
            ],
            "additionalProperties": false
          },
          "description": "Recommended; a basic placeholder is generated if omitted."
        },
        "preconditions": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "completionCriteria": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "blockerCriteria": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "memoryPolicy": {
          "type": "object",
          "properties": {
            "namespaces": {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "read": {
              "type": "boolean"
            },
            "write": {
              "type": "boolean"
            },
            "retention": {
              "type": "string"
            }
          },
          "required": [],
          "additionalProperties": false
        },
        "toolPolicy": {
          "type": "object",
          "properties": {
            "requestedTools": {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "mutatingToolsRequireApproval": {
              "type": "boolean"
            },
            "notes": {
              "type": "string"
            }
          },
          "required": [],
          "additionalProperties": false
        },
        "metadata": {
          "type": "object"
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "Server-owned; omit and the server stamps it."
        },
        "updatedAt": {
          "type": "string",
          "format": "date-time",
          "description": "Server-owned; omit and the server stamps it."
        }
      },
      "required": [
        "skillId",
        "name",
        "description",
        "instructions"
      ],
      "additionalProperties": false
    },
    "expectedWorkspaceVersion": {
      "type": "integer",
      "minimum": 0
    },
    "baseRevisionId": {
      "type": "string"
    },
    "actor": {
      "oneOf": [
        {
          "type": "string",
          "minLength": 1
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "enum": [
                "human",
                "agent",
                "system"
              ]
            },
            "id": {
              "type": "string"
            },
            "label": {
              "type": "string"
            }
          }
        }
      ]
    },
    "source": {
      "type": "string",
      "enum": [
        "mcp",
        "ui",
        "system"
      ]
    },
    "summary": {
      "type": "string"
    },
    "reason": {
      "type": "string"
    },
    "correlation": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "runId": {
          "type": "string"
        },
        "requestId": {
          "type": "string"
        }
      }
    }
  },
  "required": [
    "skill"
  ],
  "additionalProperties": false
}
```

</details>

#### `skill_delete`

Internal name: `skill.delete` · Effect: **mutate-workspace** · Autonomy: **caution**

Delete a reusable skill definition.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "skillId": {
      "type": "string",
      "minLength": 1
    }
  },
  "required": [
    "skillId"
  ],
  "additionalProperties": false
}
```

</details>

#### `skill_get`

Internal name: `skill.get` · Effect: **read** · Autonomy: **safe**

Get one reusable workspace skill.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "skillId": {
      "type": "string",
      "minLength": 1
    }
  },
  "required": [
    "skillId"
  ],
  "additionalProperties": false
}
```

</details>

#### `skill_get_version`

Internal name: `skill.get_version` · Effect: **read** · Autonomy: **safe**

Get one skill version snapshot.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "skillId": {
      "type": "string",
      "minLength": 1
    },
    "newSkillId": {
      "type": "string",
      "minLength": 1
    },
    "nodeId": {
      "type": "string",
      "minLength": 1
    },
    "versionId": {
      "type": "string",
      "minLength": 1
    },
    "skill": {},
    "patch": {
      "type": "object"
    },
    "workspaceSystemPolicy": {
      "type": "string"
    },
    "projectPolicy": {
      "type": "string"
    },
    "runInstructions": {
      "type": "string"
    },
    "platformTools": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "runAuthorizedTools": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "riskPolicy": {
      "type": "string",
      "enum": [
        "read",
        "write",
        "publish",
        "admin"
      ]
    },
    "expectedWorkspaceVersion": {
      "type": "integer",
      "minimum": 0
    },
    "baseRevisionId": {
      "type": "string"
    },
    "actor": {
      "oneOf": [
        {
          "type": "string",
          "minLength": 1
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "enum": [
                "human",
                "agent",
                "system"
              ]
            },
            "id": {
              "type": "string"
            },
            "label": {
              "type": "string"
            }
          }
        }
      ]
    },
    "source": {
      "type": "string",
      "enum": [
        "mcp",
        "ui",
        "system"
      ]
    },
    "summary": {
      "type": "string"
    },
    "reason": {
      "type": "string"
    },
    "correlation": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "runId": {
          "type": "string"
        },
        "requestId": {
          "type": "string"
        }
      }
    }
  },
  "required": [],
  "additionalProperties": false
}
```

</details>

#### `skill_list`

Internal name: `skill.list` · Effect: **read** · Autonomy: **safe**

List reusable workspace skills.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {},
  "required": [],
  "additionalProperties": false
}
```

</details>

#### `skill_list_versions`

Internal name: `skill.list_versions` · Effect: **read** · Autonomy: **safe**

List snapshots for a skill.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "skillId": {
      "type": "string",
      "minLength": 1
    }
  },
  "required": [
    "skillId"
  ],
  "additionalProperties": false
}
```

</details>

#### `skill_resolve_for_node`

Internal name: `skill.resolve_for_node` · Effect: **read** · Autonomy: **safe**

Resolve assigned skills into deterministic instructions, tools, and conflicts for a node.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "skillId": {
      "type": "string",
      "minLength": 1
    },
    "newSkillId": {
      "type": "string",
      "minLength": 1
    },
    "nodeId": {
      "type": "string",
      "minLength": 1
    },
    "versionId": {
      "type": "string",
      "minLength": 1
    },
    "skill": {},
    "patch": {
      "type": "object"
    },
    "workspaceSystemPolicy": {
      "type": "string"
    },
    "projectPolicy": {
      "type": "string"
    },
    "runInstructions": {
      "type": "string"
    },
    "platformTools": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "runAuthorizedTools": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "riskPolicy": {
      "type": "string",
      "enum": [
        "read",
        "write",
        "publish",
        "admin"
      ]
    },
    "expectedWorkspaceVersion": {
      "type": "integer",
      "minimum": 0
    },
    "baseRevisionId": {
      "type": "string"
    },
    "actor": {
      "oneOf": [
        {
          "type": "string",
          "minLength": 1
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "enum": [
                "human",
                "agent",
                "system"
              ]
            },
            "id": {
              "type": "string"
            },
            "label": {
              "type": "string"
            }
          }
        }
      ]
    },
    "source": {
      "type": "string",
      "enum": [
        "mcp",
        "ui",
        "system"
      ]
    },
    "summary": {
      "type": "string"
    },
    "reason": {
      "type": "string"
    },
    "correlation": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "runId": {
          "type": "string"
        },
        "requestId": {
          "type": "string"
        }
      }
    }
  },
  "required": [],
  "additionalProperties": false
}
```

</details>

#### `skill_restore_version`

Internal name: `skill.restore_version` · Effect: **mutate-workspace** · Autonomy: **caution**

Restore a skill from a previous version snapshot.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "skillId": {
      "type": "string",
      "minLength": 1
    },
    "newSkillId": {
      "type": "string",
      "minLength": 1
    },
    "nodeId": {
      "type": "string",
      "minLength": 1
    },
    "versionId": {
      "type": "string",
      "minLength": 1
    },
    "skill": {},
    "patch": {
      "type": "object"
    },
    "workspaceSystemPolicy": {
      "type": "string"
    },
    "projectPolicy": {
      "type": "string"
    },
    "runInstructions": {
      "type": "string"
    },
    "platformTools": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "runAuthorizedTools": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "riskPolicy": {
      "type": "string",
      "enum": [
        "read",
        "write",
        "publish",
        "admin"
      ]
    },
    "expectedWorkspaceVersion": {
      "type": "integer",
      "minimum": 0
    },
    "baseRevisionId": {
      "type": "string"
    },
    "actor": {
      "oneOf": [
        {
          "type": "string",
          "minLength": 1
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "enum": [
                "human",
                "agent",
                "system"
              ]
            },
            "id": {
              "type": "string"
            },
            "label": {
              "type": "string"
            }
          }
        }
      ]
    },
    "source": {
      "type": "string",
      "enum": [
        "mcp",
        "ui",
        "system"
      ]
    },
    "summary": {
      "type": "string"
    },
    "reason": {
      "type": "string"
    },
    "correlation": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "runId": {
          "type": "string"
        },
        "requestId": {
          "type": "string"
        }
      }
    }
  },
  "required": [],
  "additionalProperties": false
}
```

</details>

#### `skill_unassign`

Internal name: `skill.unassign` · Effect: **mutate-workspace** · Autonomy: **caution**

Remove a skill assignment from a node.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "skillId": {
      "type": "string",
      "minLength": 1
    },
    "newSkillId": {
      "type": "string",
      "minLength": 1
    },
    "nodeId": {
      "type": "string",
      "minLength": 1
    },
    "versionId": {
      "type": "string",
      "minLength": 1
    },
    "skill": {},
    "patch": {
      "type": "object"
    },
    "workspaceSystemPolicy": {
      "type": "string"
    },
    "projectPolicy": {
      "type": "string"
    },
    "runInstructions": {
      "type": "string"
    },
    "platformTools": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "runAuthorizedTools": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "riskPolicy": {
      "type": "string",
      "enum": [
        "read",
        "write",
        "publish",
        "admin"
      ]
    },
    "expectedWorkspaceVersion": {
      "type": "integer",
      "minimum": 0
    },
    "baseRevisionId": {
      "type": "string"
    },
    "actor": {
      "oneOf": [
        {
          "type": "string",
          "minLength": 1
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "enum": [
                "human",
                "agent",
                "system"
              ]
            },
            "id": {
              "type": "string"
            },
            "label": {
              "type": "string"
            }
          }
        }
      ]
    },
    "source": {
      "type": "string",
      "enum": [
        "mcp",
        "ui",
        "system"
      ]
    },
    "summary": {
      "type": "string"
    },
    "reason": {
      "type": "string"
    },
    "correlation": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "runId": {
          "type": "string"
        },
        "requestId": {
          "type": "string"
        }
      }
    }
  },
  "required": [],
  "additionalProperties": false
}
```

</details>

#### `skill_update`

Internal name: `skill.update` · Effect: **mutate-workspace** · Autonomy: **caution**

Patch a reusable skill and create a version snapshot.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "skillId": {
      "type": "string",
      "minLength": 1
    },
    "newSkillId": {
      "type": "string",
      "minLength": 1
    },
    "nodeId": {
      "type": "string",
      "minLength": 1
    },
    "versionId": {
      "type": "string",
      "minLength": 1
    },
    "skill": {},
    "patch": {
      "type": "object"
    },
    "workspaceSystemPolicy": {
      "type": "string"
    },
    "projectPolicy": {
      "type": "string"
    },
    "runInstructions": {
      "type": "string"
    },
    "platformTools": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "runAuthorizedTools": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "riskPolicy": {
      "type": "string",
      "enum": [
        "read",
        "write",
        "publish",
        "admin"
      ]
    },
    "expectedWorkspaceVersion": {
      "type": "integer",
      "minimum": 0
    },
    "baseRevisionId": {
      "type": "string"
    },
    "actor": {
      "oneOf": [
        {
          "type": "string",
          "minLength": 1
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "enum": [
                "human",
                "agent",
                "system"
              ]
            },
            "id": {
              "type": "string"
            },
            "label": {
              "type": "string"
            }
          }
        }
      ]
    },
    "source": {
      "type": "string",
      "enum": [
        "mcp",
        "ui",
        "system"
      ]
    },
    "summary": {
      "type": "string"
    },
    "reason": {
      "type": "string"
    },
    "correlation": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "runId": {
          "type": "string"
        },
        "requestId": {
          "type": "string"
        }
      }
    }
  },
  "required": [],
  "additionalProperties": false
}
```

</details>

#### `skill_validate`

Internal name: `skill.validate` · Effect: **read** · Autonomy: **safe**

Validate skill schema, tool policy, and examples.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "skillId": {
      "type": "string",
      "minLength": 1
    },
    "newSkillId": {
      "type": "string",
      "minLength": 1
    },
    "nodeId": {
      "type": "string",
      "minLength": 1
    },
    "versionId": {
      "type": "string",
      "minLength": 1
    },
    "skill": {},
    "patch": {
      "type": "object"
    },
    "workspaceSystemPolicy": {
      "type": "string"
    },
    "projectPolicy": {
      "type": "string"
    },
    "runInstructions": {
      "type": "string"
    },
    "platformTools": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "runAuthorizedTools": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "riskPolicy": {
      "type": "string",
      "enum": [
        "read",
        "write",
        "publish",
        "admin"
      ]
    },
    "expectedWorkspaceVersion": {
      "type": "integer",
      "minimum": 0
    },
    "baseRevisionId": {
      "type": "string"
    },
    "actor": {
      "oneOf": [
        {
          "type": "string",
          "minLength": 1
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "enum": [
                "human",
                "agent",
                "system"
              ]
            },
            "id": {
              "type": "string"
            },
            "label": {
              "type": "string"
            }
          }
        }
      ]
    },
    "source": {
      "type": "string",
      "enum": [
        "mcp",
        "ui",
        "system"
      ]
    },
    "summary": {
      "type": "string"
    },
    "reason": {
      "type": "string"
    },
    "correlation": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "runId": {
          "type": "string"
        },
        "requestId": {
          "type": "string"
        }
      }
    }
  },
  "required": [],
  "additionalProperties": false
}
```

</details>

### `stage`

| Wire name | Effect | Storage written | External reach | Autonomy | Note |
|---|---|---|---|---|---|
| [`stage_get_output`](#stage_get_output) | read | - | - | safe |  |
| [`stage_list_outputs`](#stage_list_outputs) | read | - | - | safe |  |
| [`stage_save_output`](#stage_save_output) | mutate-workspace | workspace/current.json (stageOutputs) | - | caution | Every save is a full-document CAS write. |

#### `stage_get_output`

Internal name: `stage.get_output` · Effect: **read** · Autonomy: **safe**

Get stage output.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "minLength": 1
    }
  },
  "required": [
    "id"
  ],
  "additionalProperties": false
}
```

</details>

#### `stage_list_outputs`

Internal name: `stage.list_outputs` · Effect: **read** · Autonomy: **safe**

List stage outputs.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "stage": {
      "type": "string",
      "minLength": 1
    }
  },
  "required": [],
  "additionalProperties": false
}
```

</details>

#### `stage_save_output`

Internal name: `stage.save_output` · Effect: **mutate-workspace** · Autonomy: **caution**

Save stage output.

> Every save is a full-document CAS write.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "minLength": 1
    },
    "stage": {
      "type": "string",
      "minLength": 1
    },
    "value": {}
  },
  "required": [
    "stage",
    "value"
  ],
  "additionalProperties": false
}
```

</details>

### `tool`

| Wire name | Effect | Storage written | External reach | Autonomy | Note |
|---|---|---|---|---|---|
| [`tool_get`](#tool_get) | read | - | - | safe |  |
| [`tool_get_effective_for_node`](#tool_get_effective_for_node) | read | - | - | safe |  |
| [`tool_get_execution`](#tool_get_execution) | read | in-process only (lost on restart) | - | safe |  |
| [`tool_list`](#tool_list) | read | - | - | safe |  |
| [`tool_list_executions`](#tool_list_executions) | read | in-process only (lost on restart) | - | safe |  |
| [`tool_test`](#tool_test) | external-read | in-process tool audit map only | whatever the controlled tool reaches (web, project MCP) | caution | Executes a controlled tool through toolExecutor; mutating tools require approval flags. |

#### `tool_get`

Internal name: `tool.get` · Effect: **read** · Autonomy: **safe**

Get one controlled tool definition.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "toolId": {
      "type": "string",
      "minLength": 1
    }
  },
  "required": [
    "toolId"
  ],
  "additionalProperties": false
}
```

</details>

#### `tool_get_effective_for_node`

Internal name: `tool.get_effective_for_node` · Effect: **read** · Autonomy: **safe**

Resolve effective controlled tools for a node.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "nodeId": {
      "type": "string",
      "minLength": 1
    },
    "runId": {
      "type": "string"
    },
    "approvedToolIds": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "runAuthorizedTools": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "platformAllowedTools": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "maxRiskLevel": {
      "type": "string",
      "enum": [
        "read",
        "write",
        "publish",
        "admin"
      ]
    }
  },
  "required": [
    "nodeId"
  ],
  "additionalProperties": false
}
```

</details>

#### `tool_get_execution`

Internal name: `tool.get_execution` · Effect: **read** · Autonomy: **safe**

Get a controlled tool execution audit record: the full in-process record when this process executed it, else the persisted per-call stub from the run record. Requires toolExecutionId; use tool.list_executions to search by run/node/tool.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "toolExecutionId": {
      "type": "string",
      "minLength": 1
    }
  },
  "required": [
    "toolExecutionId"
  ],
  "additionalProperties": false
}
```

</details>

#### `tool_list`

Internal name: `tool.list` · Effect: **read** · Autonomy: **safe**

List controlled tool registry entries.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {},
  "required": [],
  "additionalProperties": false
}
```

</details>

#### `tool_list_executions`

Internal name: `tool.list_executions` · Effect: **read** · Autonomy: **safe**

List controlled tool execution audit records by runId/nodeId/toolId — in-process records merged with the per-call stubs persisted on run records, so a past run's tool activity stays listable.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "runId": {
      "type": "string",
      "minLength": 1
    },
    "nodeId": {
      "type": "string",
      "minLength": 1
    },
    "toolId": {
      "type": "string",
      "minLength": 1
    }
  },
  "required": [],
  "additionalProperties": false
}
```

</details>

#### `tool_test`

Internal name: `tool.test` · Effect: **external-read** · Autonomy: **caution**

Execute a controlled tool through policy and audit gateway.

> Executes a controlled tool through toolExecutor; mutating tools require approval flags.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "toolId": {
      "type": "string",
      "minLength": 1
    },
    "input": {},
    "runId": {
      "type": "string"
    },
    "nodeId": {
      "type": "string",
      "minLength": 1
    },
    "projectId": {
      "type": "string"
    },
    "skillId": {
      "type": "string"
    },
    "approvedToolIds": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "runAuthorizedTools": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "platformAllowedTools": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "maxRiskLevel": {
      "type": "string",
      "enum": [
        "read",
        "write",
        "publish",
        "admin"
      ]
    }
  },
  "required": [
    "toolId",
    "nodeId"
  ],
  "additionalProperties": false
}
```

</details>

### `usage`

| Wire name | Effect | Storage written | External reach | Autonomy | Note |
|---|---|---|---|---|---|
| [`usage_get_budget_status`](#usage_get_budget_status) | read | - | - | safe |  |
| [`usage_get_summary`](#usage_get_summary) | read | - | - | safe |  |
| [`usage_list_records`](#usage_list_records) | read | - | - | safe |  |
| [`usage_record`](#usage_record) | mutate-learning | usage/ | - | caution |  |

#### `usage_get_budget_status`

Internal name: `usage.get_budget_status` · Effect: **read** · Autonomy: **safe**

Return estimated budget status for a run or project.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "projectId": {
      "type": "string",
      "minLength": 1
    },
    "runId": {
      "type": "string",
      "minLength": 1
    },
    "budgetUsd": {
      "type": "number",
      "minimum": 0
    }
  },
  "required": [],
  "additionalProperties": false
}
```

</details>

#### `usage_get_summary`

Internal name: `usage.get_summary` · Effect: **read** · Autonomy: **safe**

Summarize estimated model token and cost usage with optional filters.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "runId": {
      "type": "string",
      "minLength": 1
    },
    "projectId": {
      "type": "string",
      "minLength": 1
    },
    "workflowId": {
      "type": "string",
      "minLength": 1
    },
    "nodeId": {
      "type": "string",
      "minLength": 1
    },
    "from": {
      "type": "string",
      "format": "date-time"
    },
    "to": {
      "type": "string",
      "format": "date-time"
    },
    "status": {
      "type": "string",
      "enum": [
        "estimated",
        "actual"
      ],
      "description": "Only records of this kind: \"actual\" = measured model usage (the population budgets meter), \"estimated\" = mock/dry-run deterministic estimates (never accrue against budgetUsd)."
    }
  },
  "required": [],
  "additionalProperties": false
}
```

</details>

#### `usage_list_records`

Internal name: `usage.list_records` · Effect: **read** · Autonomy: **safe**

List model usage records with optional filters.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "runId": {
      "type": "string",
      "minLength": 1
    },
    "projectId": {
      "type": "string",
      "minLength": 1
    },
    "workflowId": {
      "type": "string",
      "minLength": 1
    },
    "nodeId": {
      "type": "string",
      "minLength": 1
    },
    "from": {
      "type": "string",
      "format": "date-time"
    },
    "to": {
      "type": "string",
      "format": "date-time"
    },
    "status": {
      "type": "string",
      "enum": [
        "estimated",
        "actual"
      ],
      "description": "Only records of this kind: \"actual\" = measured model usage (the population budgets meter), \"estimated\" = mock/dry-run deterministic estimates (never accrue against budgetUsd)."
    }
  },
  "required": [],
  "additionalProperties": false
}
```

</details>

#### `usage_record`

Internal name: `usage.record` · Effect: **mutate-learning** · Autonomy: **caution**

Record estimated or actual model usage without storing raw prompts or secrets.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "usageId": {
      "type": "string",
      "minLength": 1
    },
    "runId": {
      "type": "string",
      "minLength": 1
    },
    "workflowId": {
      "type": "string",
      "minLength": 1
    },
    "projectId": {
      "type": "string",
      "minLength": 1
    },
    "nodeId": {
      "type": "string",
      "minLength": 1
    },
    "agentId": {
      "type": "string",
      "minLength": 1
    },
    "model": {
      "type": "string",
      "minLength": 1
    },
    "provider": {
      "type": "string",
      "minLength": 1
    },
    "inputTokens": {
      "type": "integer",
      "minimum": 0
    },
    "outputTokens": {
      "type": "integer",
      "minimum": 0
    },
    "totalTokens": {
      "type": "integer",
      "minimum": 0
    },
    "reasoningTokens": {
      "type": "integer",
      "minimum": 0
    },
    "cachedInputTokens": {
      "type": "integer",
      "minimum": 0
    },
    "costUsdEstimate": {
      "type": "number",
      "minimum": 0
    },
    "currency": {
      "const": "USD"
    },
    "status": {
      "type": "string",
      "enum": [
        "estimated",
        "actual"
      ]
    },
    "recordedAt": {
      "type": "string",
      "format": "date-time"
    },
    "metadata": {
      "type": "object"
    }
  },
  "required": [
    "model",
    "provider",
    "inputTokens",
    "outputTokens",
    "status"
  ],
  "additionalProperties": false
}
```

</details>

### `visual_identity`

| Wire name | Effect | Storage written | External reach | Autonomy | Note |
|---|---|---|---|---|---|
| [`visual_identity_propose`](#visual_identity_propose) | execute-model | usage/ | vision model provider | caution | One model turn, no site write. |

#### `visual_identity_propose`

Internal name: `visual_identity.propose` · Effect: **execute-model** · Autonomy: **caution**

Propose a visual-identity artifact for ONE registered project — today, a brand_imagery_proposal.v1 from the brand_imagery_writer node. One vision model turn, zero tools, and no write to your site: the proposal comes back for an approval card and no object is created, patched, applied or published. The node is chosen by `kind` from a fixed server-side map and is never caller-supplied; `project_id` must be the caller's own project. Applying an approved proposal is a separate, owner-gated step (a visual_identity run, then site_apply_brand_imagery) that this tool cannot reach.

> One model turn, no site write.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "project_id": {
      "type": "string",
      "minLength": 1,
      "maxLength": 63,
      "description": "The caller's own project. A scoped bearer may only name a project in its own policy."
    },
    "kind": {
      "type": "string",
      "enum": [
        "brand_imagery",
        "pdf_template"
      ],
      "default": "brand_imagery",
      "description": "Which proposal to write. Only 'brand_imagery' is live; 'pdf_template' is reserved and refused."
    },
    "mode": {
      "type": "string",
      "enum": [
        "house",
        "template"
      ],
      "description": "'house' — the site's one declared look. 'template' — a named alternative an override can point at."
    },
    "visualStandardId": {
      "type": "string",
      "minLength": 1,
      "description": "An existing standard being revised, when this is a revision."
    },
    "references": {
      "type": "array",
      "maxItems": 24,
      "items": {
        "type": "object"
      },
      "description": "The mood board, in its declared shape (blobKey|url, region?, note?, weight?)."
    },
    "brief": {
      "type": "string",
      "minLength": 1,
      "maxLength": 8000,
      "description": "What the operator asked for, in words. Sufficient on its own when there is no board."
    },
    "existingBrandImagery": {
      "type": "object",
      "description": "The contract in force today, when revising rather than starting."
    },
    "templateSlug": {
      "type": "string",
      "minLength": 1,
      "maxLength": 63,
      "description": "Required for mode 'template': the <slug> in vis_<site>_<slug>."
    },
    "imageRefs": {
      "type": "array",
      "maxItems": 8,
      "items": {
        "type": "object"
      },
      "description": "BRIEF §3.9's model-visible view of the board, resolved by the caller."
    }
  },
  "required": [
    "project_id",
    "mode"
  ],
  "additionalProperties": false
}
```

</details>

### `workflow`

| Wire name | Effect | Storage written | External reach | Autonomy | Note |
|---|---|---|---|---|---|
| [`workflow_cancel_run`](#workflow_cancel_run) | mutate-run | runs/ | - | caution |  |
| [`workflow_get_run`](#workflow_get_run) | read | - | - | safe |  |
| [`workflow_get_run_context`](#workflow_get_run_context) | read | - | - | safe |  |
| [`workflow_get_run_cost`](#workflow_get_run_cost) | read | - | - | safe |  |
| [`workflow_list_runs`](#workflow_list_runs) | read | - | - | safe |  |
| [`workflow_pause_run`](#workflow_pause_run) | mutate-run | runs/ | - | caution |  |
| [`workflow_publish_readiness`](#workflow_publish_readiness) | read | - | - | safe |  |
| [`workflow_publish_run`](#workflow_publish_run) | publish | runs/ (via hooks), workspace/current.json (observation) | project MCP object_create/checkout/validate/patch/publish/checkin | operator | Five closed gates (publisher.ts). live:true + all gates → irreversible tenant write. Never releases to production (release_executor does). |
| [`workflow_reset_run`](#workflow_reset_run) | mutate-run | runs/, artifacts/ (deletes prior artifacts) | - | caution |  |
| [`workflow_resume_run`](#workflow_resume_run) | mutate-run | runs/ | - | caution |  |
| [`workflow_retry_node`](#workflow_retry_node) | execute-model | runs/, usage/ | model provider; project MCP | caution |  |
| [`workflow_run_all`](#workflow_run_all) | execute-model | runs/, artifacts/, usage/ | model provider; project MCP; may PUBLISH under autonomous policy | caution | Can reach publish_executor/release_executor when the run's policy snapshot is autonomous. |
| [`workflow_run_next_node`](#workflow_run_next_node) | execute-model | runs/, artifacts/, usage/, node_timings/, workspace/current.json (stage mirror) | model provider; project MCP; pdf-tool; web | caution | Advances one node under the per-run lock + CAS. Publish-risk nodes are gated by resolvePublishAuthority. |
| [`workflow_run_node`](#workflow_run_node) | execute-model | runs/, artifacts/, usage/ | model provider; project MCP | caution |  |
| [`workflow_run_until`](#workflow_run_until) | execute-model | runs/, artifacts/, usage/ | model provider; project MCP | caution | Bounded by RUN_DRIVER_TIME_BUDGET_MS (<=45s); the continuation tick continues the run. |
| [`workflow_set_node_budget_override`](#workflow_set_node_budget_override) | mutate-run | runs/ | - | caution |  |
| [`workflow_set_operator_publish_decision`](#workflow_set_operator_publish_decision) | mutate-run | runs/ (operatorPublishDecision) | - | operator | THE operator approval/veto. "approved" authorizes live publishing for that run. |
| [`workflow_start_dry_run`](#workflow_start_dry_run) | mutate-run | runs/, run-index/ | - | caution | Creates a run record (snapshotting publishing policy). Despite the name, executionMode defaults to "openai" and the run can publish live. |

#### `workflow_cancel_run`

Internal name: `workflow.cancel_run` · Effect: **mutate-run** · Autonomy: **caution**

cancel_run updates run status only; node completion state is never mutated.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "runId": {
      "type": "string",
      "minLength": 1
    }
  },
  "required": [
    "runId"
  ],
  "additionalProperties": false
}
```

</details>

#### `workflow_get_run`

Internal name: `workflow.get_run` · Effect: **read** · Autonomy: **safe**

Get dry-run workflow execution state. detail:"compact" (default) returns the compact run view {runId,requestId,projectId,status,currentNodeId,budget,errors,approvalsRequired,nodes:[{nodeId,status,warnings,errors,durationMs,dispatch}]}; detail:"full" returns the complete record including every node input/output, stageOutputs and artifacts (large — 100KB+ on a real run). The `mode` block reports what actually produced this run's outputs: executionMode, live (true only for real model output), and whether node definitions came from the static compile or the workspace store. For a status "running" run, `stall` reports whether anything is really in flight (dispatch heartbeat) or the driver died and the run should be advanced again.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "runId": {
      "type": "string",
      "minLength": 1
    },
    "detail": {
      "type": "string",
      "enum": [
        "compact",
        "full"
      ],
      "default": "compact",
      "description": "compact (default): the compact run view. full: the complete record including node inputs/outputs, stageOutputs and artifacts."
    }
  },
  "required": [
    "runId"
  ],
  "additionalProperties": false
}
```

</details>

#### `workflow_get_run_context`

Internal name: `workflow.get_run_context` · Effect: **read** · Autonomy: **safe**

Return the reusable per-run context bundle (project contract, article_body schema, project tool policy, object contracts, node registry), memoized per run so the conductor fetches it once instead of re-reading contracts and the registry at every step.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "runId": {
      "type": "string",
      "minLength": 1
    },
    "projectId": {
      "type": "string",
      "minLength": 1
    }
  },
  "required": [
    "runId",
    "projectId"
  ],
  "additionalProperties": false
}
```

</details>

#### `workflow_get_run_cost`

Internal name: `workflow.get_run_cost` · Effect: **read** · Autonomy: **safe**

Return a per-node cost ledger for a run plus a plan recommending the cheapest way to make progress: poll a terminal run, resume a blocked one, re-enter at the late-stage entrypoint reusing a finished article_body, or run in full. plan.nodeTimingAggregates surfaces measured per-node duration history (EMA/p50/p95/count) for this run's workflow, read-only — no plan field above it is derived from that history yet.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "runId": {
      "type": "string",
      "minLength": 1
    }
  },
  "required": [
    "runId"
  ],
  "additionalProperties": false
}
```

</details>

#### `workflow_list_runs`

Internal name: `workflow.list_runs` · Effect: **read** · Autonomy: **safe**

List compact dry-run workflow summaries, newest first, paged (default 20 rows, max 100; `page.nextCursor` fetches the next page) with optional status and startedAt time-range filters. Node inputs/outputs, stage outputs, and artifact values are intentionally omitted; call workflow.get_run for one selected run. Each row carries the caller-supplied `requestId` it was started with (when it has one), so a page of runs can be joined back to the requests that asked for them, plus a `mode` block naming what produced it and a `stall` block on status "running" rows naming whether the driver is alive.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "projectId": {
      "type": "string",
      "minLength": 1
    },
    "workflowId": {
      "type": "string",
      "minLength": 1
    },
    "status": {
      "type": "string",
      "enum": [
        "queued",
        "running",
        "paused",
        "completed",
        "failed",
        "blocked",
        "cancelled",
        "skipped"
      ],
      "description": "Only runs with exactly this status."
    },
    "from": {
      "type": "string",
      "format": "date-time",
      "description": "Only runs with startedAt >= this ISO timestamp."
    },
    "to": {
      "type": "string",
      "format": "date-time",
      "description": "Only runs with startedAt <= this ISO timestamp."
    },
    "limit": {
      "type": "integer",
      "minimum": 1,
      "maximum": 100,
      "description": "Page size; default 20, max 100."
    },
    "cursor": {
      "type": "string",
      "minLength": 1,
      "description": "Opaque nextCursor from the previous page; omit for the first page."
    }
  },
  "required": [],
  "additionalProperties": false
}
```

</details>

#### `workflow_pause_run`

Internal name: `workflow.pause_run` · Effect: **mutate-run** · Autonomy: **caution**

Pause a run: status becomes "paused" (distinct from a publish-approval or budget "blocked"); node completion state is never mutated.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "runId": {
      "type": "string",
      "minLength": 1
    }
  },
  "required": [
    "runId"
  ],
  "additionalProperties": false
}
```

</details>

#### `workflow_publish_readiness`

Internal name: `workflow.publish_readiness` · Effect: **read** · Autonomy: **safe**

Wrong-path notice: content is normally driven from the site admin chat; direct use is operator/test only. Evaluate the project's publish-readiness checklist (GO/NO-GO) for a run's client_object.v1 without publishing: client_object.v1 valid, Blob artifacts verified (pdf-tool materialized), taxonomy resolved or accepted-empty, pinned approval present, hard constraints, and release/build behavior selected. Projects without a readiness policy return available:false.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "projectId": {
      "type": "string",
      "minLength": 1
    },
    "runId": {
      "type": "string",
      "minLength": 1
    },
    "articleBody": {
      "type": "object",
      "description": "Article body to evaluate; omit to resolve it from the run. Judged by the project's readiness policy against the article_body node's own outputSchema, never a workspace-local article schema."
    },
    "readiness": {
      "type": "object",
      "properties": {
        "verifiedMediaRefs": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Artifact refs confirmed pdf-tool materialized for this request (e.g. from list_artifacts_for_request/verify_article_images). A Blob-shaped media src not listed here is treated as unverified."
        },
        "taxonomy": {
          "type": "object",
          "properties": {
            "tags": {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "acceptedEmpty": {
              "type": "boolean"
            }
          },
          "required": [],
          "additionalProperties": false
        },
        "approval": {
          "type": "object",
          "properties": {
            "pinned": {
              "type": "boolean"
            },
            "approvedBy": {
              "type": "string"
            },
            "approvedAt": {
              "type": "string"
            }
          },
          "required": [],
          "additionalProperties": false
        },
        "releaseBehavior": {
          "type": "string",
          "description": "publish_now | schedule | build_only | unpublish."
        },
        "hardConstraints": {
          "type": "object",
          "properties": {
            "contentPath": {
              "type": "string"
            },
            "artifactProtocol": {
              "type": "string"
            },
            "legacyFallbacksUsed": {
              "type": "boolean"
            }
          },
          "required": [],
          "additionalProperties": false
        }
      },
      "required": [],
      "additionalProperties": false
    }
  },
  "required": [
    "projectId"
  ],
  "additionalProperties": false
}
```

</details>

#### `workflow_publish_run`

Internal name: `workflow.publish_run` · Effect: **publish** · Autonomy: **operator**

Wrong-path notice: content is normally driven from the site admin chat; direct use is operator/test only, and is not the way autonomy is exercised. Explicit PUBLISH gate: publish a run's client_object.v1 to the project's live site via that project's own sanctioned publish dialect (for object-substrate clients: object_create -> object_checkout -> object_validate -> object_patch -> object_publish -> object_checkin; the exact sequence is reported as plan.toolSequence). Never releases to production — going live is a separate, explicit gate. A real publish requires operator-enabled publishing (a per-project env flag), live:true, the run's publish authority resolved AUTHORIZED (an explicit operator approval via workflow.set_operator_publish_decision, or the project's autonomyMode policy — see publishDecision.resolvePublishAuthority), and the project's publish-readiness policy must be GO. approved is DEPRECATED as an authority input (accepted for compatibility, no longer consulted) and can never override an operator's "withheld". A readiness NO-GO returns mode blocked_for_publish_execution (an expected, resumable safety state); missing gates return a dry-run plan. Text-only bodies only.

> Five closed gates (publisher.ts). live:true + all gates → irreversible tenant write. Never releases to production (release_executor does).

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "runId": {
      "type": "string",
      "minLength": 1
    },
    "projectId": {
      "type": "string",
      "minLength": 1
    },
    "requestId": {
      "type": "string",
      "minLength": 1,
      "description": "req_<flow>_<topic>_<yyyymmdd>_<nn>, lowercase snake_case; you supply it."
    },
    "approved": {
      "type": "boolean",
      "description": "Explicit human approval; required (with live) for a real publish."
    },
    "live": {
      "type": "boolean",
      "description": "Must be true — with approved:true and operator-enabled publishing — for a real publish; otherwise a dry-run plan is returned and nothing external is called."
    },
    "publishedTime": {
      "type": "string",
      "description": "Optional ISO timestamp: omit/past publishes now, future schedules."
    },
    "readiness": {
      "type": "object",
      "properties": {
        "verifiedMediaRefs": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Artifact refs confirmed pdf-tool materialized for this request (e.g. from list_artifacts_for_request/verify_article_images). A Blob-shaped media src not listed here is treated as unverified."
        },
        "taxonomy": {
          "type": "object",
          "properties": {
            "tags": {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "acceptedEmpty": {
              "type": "boolean"
            }
          },
          "required": [],
          "additionalProperties": false
        },
        "approval": {
          "type": "object",
          "properties": {
            "pinned": {
              "type": "boolean"
            },
            "approvedBy": {
              "type": "string"
            },
            "approvedAt": {
              "type": "string"
            }
          },
          "required": [],
          "additionalProperties": false
        },
        "releaseBehavior": {
          "type": "string",
          "description": "publish_now | schedule | build_only | unpublish."
        },
        "hardConstraints": {
          "type": "object",
          "properties": {
            "contentPath": {
              "type": "string"
            },
            "artifactProtocol": {
              "type": "string"
            },
            "legacyFallbacksUsed": {
              "type": "boolean"
            }
          },
          "required": [],
          "additionalProperties": false
        }
      },
      "required": [],
      "additionalProperties": false
    }
  },
  "required": [
    "runId",
    "requestId"
  ],
  "additionalProperties": false
}
```

</details>

#### `workflow_reset_run`

Internal name: `workflow.reset_run` · Effect: **mutate-run** · Autonomy: **caution**

Reset a dry-run workflow execution to its initial queued state. The run's requestId, the operator's durable publish decision, and its operator-supplied publishRequestId all survive the reset — a reset retries the same request rather than starting a new one.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "runId": {
      "type": "string",
      "minLength": 1
    }
  },
  "required": [
    "runId"
  ],
  "additionalProperties": false
}
```

</details>

#### `workflow_resume_run`

Internal name: `workflow.resume_run` · Effect: **mutate-run** · Autonomy: **caution**

Resume a run: status becomes "queued". Optionally raise (or set) budgetUsd in the same call — the reachable form of the budget gate's own "raise budgetUsd and resume" remedy. Node completion state is never mutated.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "runId": {
      "type": "string",
      "minLength": 1
    },
    "budgetUsd": {
      "type": "number",
      "minimum": 0,
      "description": "Optional: raise (or set) the run's per-run cost ceiling in the same call that resumes it. Omit to resume unchanged — this is what makes the budget gate's own remedy (\"raise budgetUsd and resume\") actually reachable; previously resume_run took only runId and there was no way to raise the ceiling that blocked the run."
    }
  },
  "required": [
    "runId"
  ],
  "additionalProperties": false
}
```

</details>

#### `workflow_retry_node`

Internal name: `workflow.retry_node` · Effect: **execute-model** · Autonomy: **caution**

Wrong-path notice: content is normally driven from the site admin chat; direct use is operator/test only. Reset a completed or failed node back to queued and run the next dependency-ready node.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "runId": {
      "type": "string",
      "minLength": 1
    },
    "nodeId": {
      "type": "string",
      "minLength": 1
    },
    "approved": {
      "type": "boolean"
    }
  },
  "required": [
    "runId"
  ],
  "additionalProperties": false
}
```

</details>

#### `workflow_run_all`

Internal name: `workflow.run_all` · Effect: **execute-model** · Autonomy: **caution**

Wrong-path notice: content is normally driven from the site admin chat; direct use is operator/test only. Run all dependency-ready nodes, stopping before publish-risk nodes unless explicit approval exists. Drives the run for at most budgetMs (default 45000ms, ceiling 45000ms — always below the caller's request timeout) and returns a COMPACT run view {run:{runId,requestId,projectId,status,currentNodeId,budget,errors,approvalsRequired,nodes:[{nodeId,status,warnings,errors,durationMs,dispatch}]}, driverNote?, continued} — no node inputs/outputs/stageOutputs/artifacts (use workflow.get_run for the full record). continued=true means the run is still queued/running and the scheduled continuation tick will advance it; call again to drive it sooner.

> Can reach publish_executor/release_executor when the run's policy snapshot is autonomous.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "runId": {
      "type": "string",
      "minLength": 1
    },
    "approved": {
      "type": "boolean"
    },
    "budgetMs": {
      "type": "number",
      "minimum": 5000,
      "maximum": 45000,
      "description": "Wall-clock budget for THIS call in ms (5000..45000); default 45000. The loop stops dispatching when it is reached and the run continues on the scheduled continuation tick."
    }
  },
  "required": [
    "runId"
  ],
  "additionalProperties": false
}
```

</details>

#### `workflow_run_next_node`

Internal name: `workflow.run_next_node` · Effect: **execute-model** · Autonomy: **caution**

Run exactly one dependency-ready Publishing Conductor node, stopping before publish-risk nodes unless approved is true.

> Advances one node under the per-run lock + CAS. Publish-risk nodes are gated by resolvePublishAuthority.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "runId": {
      "type": "string",
      "minLength": 1
    },
    "approved": {
      "type": "boolean"
    }
  },
  "required": [
    "runId"
  ],
  "additionalProperties": false
}
```

</details>

#### `workflow_run_node`

Internal name: `workflow.run_node` · Effect: **execute-model** · Autonomy: **caution**

Wrong-path notice: content is normally driven from the site admin chat; direct use is operator/test only. Run dependency-ready nodes; when nodeId is given, advance the run until that node completes. Stops cleanly with driverNote when the request's time budget runs out; call again to continue, or use the conductor job for long runs.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "runId": {
      "type": "string",
      "minLength": 1
    },
    "nodeId": {
      "type": "string",
      "minLength": 1
    },
    "approved": {
      "type": "boolean"
    }
  },
  "required": [
    "runId"
  ],
  "additionalProperties": false
}
```

</details>

#### `workflow_run_until`

Internal name: `workflow.run_until` · Effect: **execute-model** · Autonomy: **caution**

Wrong-path notice: content is normally driven from the site admin chat; direct use is operator/test only. Run dependency-ready nodes until the named node completes, then stop. Stops cleanly with driverNote when the request's time budget runs out; call again to continue, or use the conductor job for long runs.

> Bounded by RUN_DRIVER_TIME_BUDGET_MS (<=45s); the continuation tick continues the run.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "runId": {
      "type": "string",
      "minLength": 1
    },
    "nodeId": {
      "type": "string",
      "minLength": 1
    },
    "approved": {
      "type": "boolean"
    }
  },
  "required": [
    "runId",
    "nodeId"
  ],
  "additionalProperties": false
}
```

</details>

#### `workflow_set_node_budget_override`

Internal name: `workflow.set_node_budget_override` · Effect: **mutate-run** · Autonomy: **caution**

Wrong-path notice: content is normally driven from the site admin chat; direct use is operator/test only. Set a per-run override for one node's budget ceiling (run.nodeBudgetOverrides[nodeId]) — read by the budget guard in PREFERENCE to the node's own modelConfig.budgetUsd, for THIS run only; the node's stored modelConfig is never touched, so every other run keeps the node's normal ceiling. Use a budget_exceeded error's own details.suggestedBudgetUsd as the figure. Does NOT retry the node — call workflow.retry_node afterward to actually re-run it under the new ceiling.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "runId": {
      "type": "string",
      "minLength": 1
    },
    "nodeId": {
      "type": "string",
      "minLength": 1
    },
    "budgetUsd": {
      "type": "number",
      "exclusiveMinimum": 0,
      "description": "The node's new per-run budget ceiling in USD, in place of its own modelConfig.budgetUsd for THIS run only — see a budget_exceeded error's own details.suggestedBudgetUsd for a computed raise. The node's stored modelConfig is never touched (every other run keeps its normal ceiling), and this does NOT retry the node — call workflow.retry_node separately once the override is set."
    }
  },
  "required": [
    "runId",
    "nodeId",
    "budgetUsd"
  ],
  "additionalProperties": false
}
```

</details>

#### `workflow_set_operator_publish_decision`

Internal name: `workflow.set_operator_publish_decision` · Effect: **mutate-run** · Autonomy: **operator**

Wrong-path notice: content is normally driven from the site admin chat; direct use is operator/test only. Record the operator's durable publish decision for a run (run.operatorPublishDecision) — the ONLY thing that ever writes this field, in every project autonomy mode. "withheld" is the operator VETO: it blocks workflow.publish_run and every publish-risk node for this run regardless of approved/live flags or the project's autonomyMode policy, until the operator replaces it. "approved" records explicit durable operator approval — the referent an executed publish_execution.v1's approvalMatched must match. The decision survives workflow.reset_run.

> THE operator approval/veto. "approved" authorizes live publishing for that run.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "runId": {
      "type": "string",
      "minLength": 1
    },
    "decision": {
      "type": "string",
      "enum": [
        "approved",
        "withheld"
      ],
      "description": "\"withheld\" is a durable operator veto: it blocks workflow.publish_run and every publish-risk node for this run regardless of approved/live flags, until replaced. \"approved\" records explicit, durable operator approval — the record an executed publish_execution.v1's approvalMatched must match."
    }
  },
  "required": [
    "runId",
    "decision"
  ],
  "additionalProperties": false
}
```

</details>

#### `workflow_start_dry_run`

Internal name: `workflow.start_dry_run` · Effect: **mutate-run** · Autonomy: **caution**

Wrong-path notice: content is normally driven from the site admin chat; direct use is operator/test only. Start a Publishing Conductor dry-run workflow without external MCP calls or publishing side effects. Supply entrypoint 'article_body' with a valid client_object.v1 to enter the run at the publish stages without re-running ideation/research/draft nodes. Live (openai) runs for projects that declare a request-id pattern (platform, dr-lurie, fernwell) REQUIRE a caller-supplied requestId (req_<flow>_<topic>_<yyyymmdd>_<nn>); the tool refuses with request_id_required / invalid_request_id otherwise. Mock dry-runs keep the auto-minted id. Supply `publishRequestId` (a DIFFERENT id: the operator-authored publish contract id normally written by artifact_plan) to let a late-stage entrypoint run reach the publish gate — without it such a run is refused with publish_request_id_absent, and no publish id is ever generated.

> Creates a run record (snapshotting publishing policy). Despite the name, executionMode defaults to "openai" and the run can publish live.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "projectId": {
      "type": "string",
      "minLength": 1
    },
    "input": {},
    "workflowId": {
      "type": "string",
      "minLength": 1
    },
    "executionMode": {
      "type": "string",
      "enum": [
        "mock",
        "openai"
      ],
      "default": "openai",
      "description": "Execution mode. \"openai\" (DEFAULT) calls the configured model provider and produces real node output. \"mock\" produces deterministic placeholder output generated from each node's outputSchema — structurally valid but content-free, for cheap CI/test runs; mock artifacts must never be treated as publishable content. Every run reports its mode back on workflow.get_run / workflow.list_runs as `mode`."
    },
    "entrypoint": {
      "type": "string",
      "enum": [
        "article_body"
      ],
      "description": "Late-stage entrypoint. With a supplied valid articleBody the run enters at article_body -> publish_payload -> publication_controller and earlier ideation/research/draft nodes are seeded as completed (not re-run)."
    },
    "articleBody": {
      "type": "object",
      "description": "Output to seed as the article_body node's result for a late-stage entrypoint run. Validated against the article_body node's OWN outputSchema (see node.get_output_schema) — not against a workspace-local article shape, which the node rejects. Rejected before the run is created, with the failing fields named."
    },
    "budgetUsd": {
      "type": "number",
      "minimum": 0,
      "description": "Optional per-run cost ceiling in USD. Default OFF (omit = no gate). When set, the conductor halts the run (status blocked, paused for budget) before dispatching any node once the run's accrued estimated model cost reaches this ceiling; the pending node is not executed. Inspect via workflow.get_run_cost (ledger.budget)."
    },
    "requestId": {
      "type": "string",
      "minLength": 1,
      "description": "Caller-supplied request id for this run. REQUIRED for a live (openai) run when the project declares objectDialect.requestIdPattern (platform, dr-lurie, fernwell: req_<flow>_<topic>_<yyyymmdd>_<nn>, lowercase snake_case) — the tool refuses with request_id_required/invalid_request_id naming the pattern; request ids are never auto-generated for such a run. Optional (auto-minted) for a mock dry-run or a project without a pattern; a supplied id is always validated."
    },
    "publishRequestId": {
      "type": "string",
      "minLength": 1,
      "description": "Operator-supplied PUBLISH request id (req_<flow>_<topic>_<yyyymmdd>_<nn>, lowercase snake_case), stored on the run and lifted into every node's run context. A DIFFERENT identifier from `requestId`, which is the platform/workspace join key — neither ever substitutes for the other. This id is normally authored by the artifact_plan node; supply it here for a late-stage entrypoint run, whose artifact_plan is seeded as skipped and therefore authors none (without it such a run reaches the publish gate and is refused with publish_request_id_absent). Always OPTIONAL and never generated: omit it and the run simply has no publish id and that refusal stands. A supplied id is validated before the run is created against the project's objectDialect.requestIdPattern where declared (platform, dr-lurie, fernwell), otherwise the publisher's shared contract pattern, refusing with invalid_publish_request_id. An id authored by a real artifact_plan run always takes precedence over this one. Survives workflow.reset_run."
    }
  },
  "required": [
    "projectId",
    "input"
  ],
  "additionalProperties": false
}
```

</details>

### `workspace`

| Wire name | Effect | Storage written | External reach | Autonomy | Note |
|---|---|---|---|---|---|
| [`workspace_clone_node`](#workspace_clone_node) | mutate-workspace | workspace/current.json, changes/, revisions/ | - | caution |  |
| [`workspace_create_node`](#workspace_create_node) | mutate-workspace | workspace/current.json, changes/, revisions/ | - | caution |  |
| [`workspace_delete_node`](#workspace_delete_node) | mutate-workspace | workspace/current.json, changes/, revisions/ | - | operator | Canonical nodes cannot be removed without adminApproved+allowCanonicalNodeRemoval. |
| [`workspace_export_workspace`](#workspace_export_workspace) | read | - | - | safe | Returns the whole document incl. stage outputs and observations (can be large). |
| [`workspace_get_graph`](#workspace_get_graph) | read | - | - | safe |  |
| [`workspace_get_node`](#workspace_get_node) | read | - | - | safe |  |
| [`workspace_get_node_effective_config`](#workspace_get_node_effective_config) | read | - | - | safe |  |
| [`workspace_get_nodes`](#workspace_get_nodes) | read | - | - | safe |  |
| [`workspace_import_workspace`](#workspace_import_workspace) | mutate-workspace | workspace/current.json, changes/, revisions/ | - | operator | Upserts nodes/stage outputs/observations by id with no version check. |
| [`workspace_reorder_nodes`](#workspace_reorder_nodes) | mutate-workspace | workspace/current.json, changes/, revisions/ | - | caution |  |
| [`workspace_update_graph`](#workspace_update_graph) | mutate-workspace | workspace/current.json, changes/, revisions/ | - | operator |  |
| [`workspace_update_node`](#workspace_update_node) | mutate-workspace | workspace/current.json, changes/, revisions/ | - | caution | Store-mode overlay: prompt/schema/tools/model/metadata changes reach the NEXT run of every workflow sharing the node id. |
| [`workspace_update_node_dependencies`](#workspace_update_node_dependencies) | mutate-workspace | workspace/current.json, changes/, revisions/ | - | caution | Topology is PINNED to canonical at run time (overlayStoreNode); a store dependsOn edit does not change what runs. |
| [`workspace_update_node_input_schema`](#workspace_update_node_input_schema) | mutate-workspace | workspace/current.json, changes/, revisions/ | - | caution |  |
| [`workspace_update_node_metadata`](#workspace_update_node_metadata) | mutate-workspace | workspace/current.json, changes/, revisions/ | - | operator | Metadata carries the *Deterministic route flags (incl. publishExecutorDeterministic). This tool replaces the row's metadata field; the run-time overlay merges it per key with canonical. |
| [`workspace_update_node_model_config`](#workspace_update_node_model_config) | mutate-workspace | workspace/current.json, changes/, revisions/ | - | caution |  |
| [`workspace_update_node_output_schema`](#workspace_update_node_output_schema) | mutate-workspace | workspace/current.json, changes/, revisions/ | - | caution | Changes what the executor validates model output against; publisher.ts still validates article_body against the CANONICAL schema. |
| [`workspace_update_node_prompt`](#workspace_update_node_prompt) | mutate-workspace | workspace/current.json, changes/, revisions/ | - | caution |  |
| [`workspace_update_node_skills`](#workspace_update_node_skills) | mutate-workspace | workspace/current.json, changes/, revisions/ | - | caution |  |
| [`workspace_update_node_tools`](#workspace_update_node_tools) | mutate-workspace | workspace/current.json, changes/, revisions/ | - | operator | Changing tool grants on a publish/admin node is an operator decision (canonical publish nodes already carry project.call_tool); this tool records only change history as a guard. |
| [`workspace_update_relationships`](#workspace_update_relationships) | mutate-workspace | workspace/current.json, changes/, revisions/ | - | caution |  |
| [`workspace_validate_graph`](#workspace_validate_graph) | read | - | - | safe |  |
| [`workspace_validate_node`](#workspace_validate_node) | read | - | - | safe |  |

#### `workspace_clone_node`

Internal name: `workspace.clone_node` · Effect: **mutate-workspace** · Autonomy: **caution**

Clone a workspace node.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "minLength": 1
    },
    "newId": {
      "type": "string",
      "minLength": 1
    },
    "node": {},
    "patch": {
      "type": "object"
    },
    "create": {
      "type": "array"
    },
    "update": {
      "type": "array"
    },
    "delete": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "dependencies": {
      "type": "object"
    },
    "orderedNodeIds": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "positions": {
      "type": "object"
    },
    "expectedWorkspaceVersion": {
      "type": "integer",
      "minimum": 0
    },
    "baseRevisionId": {
      "type": "string"
    },
    "actor": {
      "oneOf": [
        {
          "type": "string",
          "minLength": 1
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "enum": [
                "human",
                "agent",
                "system"
              ]
            },
            "id": {
              "type": "string"
            },
            "label": {
              "type": "string"
            }
          }
        }
      ]
    },
    "source": {
      "type": "string",
      "enum": [
        "mcp",
        "ui",
        "system"
      ]
    },
    "summary": {
      "type": "string"
    },
    "reason": {
      "type": "string"
    },
    "correlation": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "runId": {
          "type": "string"
        },
        "requestId": {
          "type": "string"
        }
      }
    }
  },
  "required": [],
  "additionalProperties": false
}
```

</details>

#### `workspace_create_node`

Internal name: `workspace.create_node` · Effect: **mutate-workspace** · Autonomy: **caution**

Create a workspace node.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "minLength": 1
    },
    "newId": {
      "type": "string",
      "minLength": 1
    },
    "node": {},
    "patch": {
      "type": "object"
    },
    "create": {
      "type": "array"
    },
    "update": {
      "type": "array"
    },
    "delete": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "dependencies": {
      "type": "object"
    },
    "orderedNodeIds": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "positions": {
      "type": "object"
    },
    "expectedWorkspaceVersion": {
      "type": "integer",
      "minimum": 0
    },
    "baseRevisionId": {
      "type": "string"
    },
    "actor": {
      "oneOf": [
        {
          "type": "string",
          "minLength": 1
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "enum": [
                "human",
                "agent",
                "system"
              ]
            },
            "id": {
              "type": "string"
            },
            "label": {
              "type": "string"
            }
          }
        }
      ]
    },
    "source": {
      "type": "string",
      "enum": [
        "mcp",
        "ui",
        "system"
      ]
    },
    "summary": {
      "type": "string"
    },
    "reason": {
      "type": "string"
    },
    "correlation": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "runId": {
          "type": "string"
        },
        "requestId": {
          "type": "string"
        }
      }
    }
  },
  "required": [],
  "additionalProperties": false
}
```

</details>

#### `workspace_delete_node`

Internal name: `workspace.delete_node` · Effect: **mutate-workspace** · Autonomy: **operator**

Delete an unreferenced workspace node.

> Canonical nodes cannot be removed without adminApproved+allowCanonicalNodeRemoval.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "minLength": 1
    },
    "newId": {
      "type": "string",
      "minLength": 1
    },
    "node": {},
    "patch": {
      "type": "object"
    },
    "create": {
      "type": "array"
    },
    "update": {
      "type": "array"
    },
    "delete": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "dependencies": {
      "type": "object"
    },
    "orderedNodeIds": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "positions": {
      "type": "object"
    },
    "expectedWorkspaceVersion": {
      "type": "integer",
      "minimum": 0
    },
    "baseRevisionId": {
      "type": "string"
    },
    "actor": {
      "oneOf": [
        {
          "type": "string",
          "minLength": 1
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "enum": [
                "human",
                "agent",
                "system"
              ]
            },
            "id": {
              "type": "string"
            },
            "label": {
              "type": "string"
            }
          }
        }
      ]
    },
    "source": {
      "type": "string",
      "enum": [
        "mcp",
        "ui",
        "system"
      ]
    },
    "summary": {
      "type": "string"
    },
    "reason": {
      "type": "string"
    },
    "correlation": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "runId": {
          "type": "string"
        },
        "requestId": {
          "type": "string"
        }
      }
    }
  },
  "required": [],
  "additionalProperties": false
}
```

</details>

#### `workspace_export_workspace`

Internal name: `workspace.export_workspace` · Effect: **read** · Autonomy: **safe**

Export workspace data.

> Returns the whole document incl. stage outputs and observations (can be large).

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {},
  "required": [],
  "additionalProperties": false
}
```

</details>

#### `workspace_get_graph`

Internal name: `workspace.get_graph` · Effect: **read** · Autonomy: **safe**

Get workflow graph nodes and edges. Omit workflowId for the flat store view of every registered workflow's nodes merged together (publishing_conductor, capture_conductor, clone_conductor); pass a registered workflowId to get that ONE workflow's actual run topology instead (canonical dependsOn — e.g. capture_conductor's publish_payload bound to capture_emit_live/capture_score — overlaid with store-edited prompt/schema/tools, exactly what resolveConductorNodes hands the executor).

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "workflowId": {
      "type": "string",
      "minLength": 1,
      "description": "Optional. A registered workflow id (\"publishing_conductor\", \"capture_conductor\", \"clone_conductor\") to get that workflow's ACTUAL run topology (canonical dependsOn, store-overlaid prompt/schema/tools) instead of the flat store view. Omit for the flat store view of every governance-visible node."
    }
  },
  "required": [],
  "additionalProperties": false
}
```

</details>

#### `workspace_get_node`

Internal name: `workspace.get_node` · Effect: **read** · Autonomy: **safe**

Get one workspace node.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "minLength": 1
    }
  },
  "required": [
    "id"
  ],
  "additionalProperties": false
}
```

</details>

#### `workspace_get_node_effective_config`

Internal name: `workspace.get_node_effective_config` · Effect: **read** · Autonomy: **safe**

Get safe resolved node execution config without secrets. Resolves for capture_conductor's and clone_conductor's own nodes as well as publishing_conductor's.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "minLength": 1
    }
  },
  "required": [
    "id"
  ],
  "additionalProperties": false
}
```

</details>

#### `workspace_get_nodes`

Internal name: `workspace.get_nodes` · Effect: **read** · Autonomy: **safe**

List workspace nodes. Omit workflowId for every governance-visible node across every registered workflow (publishing_conductor, capture_conductor, clone_conductor); pass a registered workflowId to scope to that ONE conductor's actual node set instead (same resolution as workspace.get_graph's workflowId — canonical dependsOn overlaid with store-edited prompt/schema/tools).

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "workflowId": {
      "type": "string",
      "minLength": 1,
      "description": "Optional. A registered workflow id (\"publishing_conductor\", \"capture_conductor\", \"clone_conductor\") to get that workflow's ACTUAL run topology (canonical dependsOn, store-overlaid prompt/schema/tools) instead of the flat store view. Omit for the flat store view of every governance-visible node."
    }
  },
  "required": [],
  "additionalProperties": false
}
```

</details>

#### `workspace_import_workspace`

Internal name: `workspace.import_workspace` · Effect: **mutate-workspace** · Autonomy: **operator**

Import workspace data.

> Upserts nodes/stage outputs/observations by id with no version check.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "nodes": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "minLength": 1
          },
          "name": {
            "type": "string",
            "minLength": 1
          },
          "prompt": {
            "type": "string"
          },
          "schema": {},
          "updatedAt": {
            "type": "string",
            "format": "date-time"
          }
        },
        "required": [
          "id",
          "name",
          "prompt",
          "schema",
          "updatedAt"
        ],
        "additionalProperties": false
      }
    },
    "stageOutputs": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "minLength": 1
          },
          "stage": {
            "type": "string",
            "minLength": 1
          },
          "value": {},
          "createdAt": {
            "type": "string",
            "format": "date-time"
          }
        },
        "required": [
          "id",
          "stage",
          "value",
          "createdAt"
        ],
        "additionalProperties": false
      }
    },
    "learningObservations": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string",
            "minLength": 1
          },
          "observation": {
            "type": "string",
            "minLength": 1
          },
          "metadata": {
            "type": "object"
          },
          "createdAt": {
            "type": "string",
            "format": "date-time"
          }
        },
        "required": [
          "id",
          "observation",
          "createdAt"
        ],
        "additionalProperties": false
      }
    }
  },
  "required": [],
  "additionalProperties": false
}
```

</details>

#### `workspace_reorder_nodes`

Internal name: `workspace.reorder_nodes` · Effect: **mutate-workspace** · Autonomy: **caution**

Reorder nodes without changing dependencies.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "minLength": 1
    },
    "newId": {
      "type": "string",
      "minLength": 1
    },
    "node": {},
    "patch": {
      "type": "object"
    },
    "create": {
      "type": "array"
    },
    "update": {
      "type": "array"
    },
    "delete": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "dependencies": {
      "type": "object"
    },
    "orderedNodeIds": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "positions": {
      "type": "object"
    },
    "expectedWorkspaceVersion": {
      "type": "integer",
      "minimum": 0
    },
    "baseRevisionId": {
      "type": "string"
    },
    "actor": {
      "oneOf": [
        {
          "type": "string",
          "minLength": 1
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "enum": [
                "human",
                "agent",
                "system"
              ]
            },
            "id": {
              "type": "string"
            },
            "label": {
              "type": "string"
            }
          }
        }
      ]
    },
    "source": {
      "type": "string",
      "enum": [
        "mcp",
        "ui",
        "system"
      ]
    },
    "summary": {
      "type": "string"
    },
    "reason": {
      "type": "string"
    },
    "correlation": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "runId": {
          "type": "string"
        },
        "requestId": {
          "type": "string"
        }
      }
    }
  },
  "required": [],
  "additionalProperties": false
}
```

</details>

#### `workspace_update_graph`

Internal name: `workspace.update_graph` · Effect: **mutate-workspace** · Autonomy: **operator**

Atomically update workflow graph.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "minLength": 1
    },
    "newId": {
      "type": "string",
      "minLength": 1
    },
    "node": {},
    "patch": {
      "type": "object"
    },
    "create": {
      "type": "array"
    },
    "update": {
      "type": "array"
    },
    "delete": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "dependencies": {
      "type": "object"
    },
    "orderedNodeIds": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "positions": {
      "type": "object"
    },
    "expectedWorkspaceVersion": {
      "type": "integer",
      "minimum": 0
    },
    "baseRevisionId": {
      "type": "string"
    },
    "actor": {
      "oneOf": [
        {
          "type": "string",
          "minLength": 1
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "enum": [
                "human",
                "agent",
                "system"
              ]
            },
            "id": {
              "type": "string"
            },
            "label": {
              "type": "string"
            }
          }
        }
      ]
    },
    "source": {
      "type": "string",
      "enum": [
        "mcp",
        "ui",
        "system"
      ]
    },
    "summary": {
      "type": "string"
    },
    "reason": {
      "type": "string"
    },
    "correlation": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "runId": {
          "type": "string"
        },
        "requestId": {
          "type": "string"
        }
      }
    }
  },
  "required": [],
  "additionalProperties": false
}
```

</details>

#### `workspace_update_node`

Internal name: `workspace.update_node` · Effect: **mutate-workspace** · Autonomy: **caution**

Patch a workspace node.

> Store-mode overlay: prompt/schema/tools/model/metadata changes reach the NEXT run of every workflow sharing the node id.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "minLength": 1
    },
    "newId": {
      "type": "string",
      "minLength": 1
    },
    "node": {},
    "patch": {
      "type": "object"
    },
    "create": {
      "type": "array"
    },
    "update": {
      "type": "array"
    },
    "delete": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "dependencies": {
      "type": "object"
    },
    "orderedNodeIds": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "positions": {
      "type": "object"
    },
    "expectedWorkspaceVersion": {
      "type": "integer",
      "minimum": 0
    },
    "baseRevisionId": {
      "type": "string"
    },
    "actor": {
      "oneOf": [
        {
          "type": "string",
          "minLength": 1
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "enum": [
                "human",
                "agent",
                "system"
              ]
            },
            "id": {
              "type": "string"
            },
            "label": {
              "type": "string"
            }
          }
        }
      ]
    },
    "source": {
      "type": "string",
      "enum": [
        "mcp",
        "ui",
        "system"
      ]
    },
    "summary": {
      "type": "string"
    },
    "reason": {
      "type": "string"
    },
    "correlation": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "runId": {
          "type": "string"
        },
        "requestId": {
          "type": "string"
        }
      }
    }
  },
  "required": [],
  "additionalProperties": false
}
```

</details>

#### `workspace_update_node_dependencies`

Internal name: `workspace.update_node_dependencies` · Effect: **mutate-workspace** · Autonomy: **caution**

Update node dependsOn.

> Topology is PINNED to canonical at run time (overlayStoreNode); a store dependsOn edit does not change what runs.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "minLength": 1
    },
    "newId": {
      "type": "string",
      "minLength": 1
    },
    "node": {},
    "patch": {
      "type": "object"
    },
    "create": {
      "type": "array"
    },
    "update": {
      "type": "array"
    },
    "delete": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "dependencies": {
      "type": "object"
    },
    "orderedNodeIds": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "positions": {
      "type": "object"
    },
    "expectedWorkspaceVersion": {
      "type": "integer",
      "minimum": 0
    },
    "baseRevisionId": {
      "type": "string"
    },
    "actor": {
      "oneOf": [
        {
          "type": "string",
          "minLength": 1
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "enum": [
                "human",
                "agent",
                "system"
              ]
            },
            "id": {
              "type": "string"
            },
            "label": {
              "type": "string"
            }
          }
        }
      ]
    },
    "source": {
      "type": "string",
      "enum": [
        "mcp",
        "ui",
        "system"
      ]
    },
    "summary": {
      "type": "string"
    },
    "reason": {
      "type": "string"
    },
    "correlation": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "runId": {
          "type": "string"
        },
        "requestId": {
          "type": "string"
        }
      }
    }
  },
  "required": [],
  "additionalProperties": false
}
```

</details>

#### `workspace_update_node_input_schema`

Internal name: `workspace.update_node_input_schema` · Effect: **mutate-workspace** · Autonomy: **caution**

Update node input JSON Schema.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "minLength": 1
    },
    "schema": {
      "type": [
        "object",
        "boolean"
      ]
    },
    "expectedWorkspaceVersion": {
      "type": "integer",
      "minimum": 0
    },
    "baseRevisionId": {
      "type": "string"
    },
    "actor": {
      "oneOf": [
        {
          "type": "string",
          "minLength": 1
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "enum": [
                "human",
                "agent",
                "system"
              ]
            },
            "id": {
              "type": "string"
            },
            "label": {
              "type": "string"
            }
          }
        }
      ]
    },
    "source": {
      "type": "string",
      "enum": [
        "mcp",
        "ui",
        "system"
      ]
    },
    "summary": {
      "type": "string"
    },
    "reason": {
      "type": "string"
    },
    "correlation": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "runId": {
          "type": "string"
        },
        "requestId": {
          "type": "string"
        }
      }
    }
  },
  "required": [
    "id",
    "schema"
  ],
  "additionalProperties": false
}
```

</details>

#### `workspace_update_node_metadata`

Internal name: `workspace.update_node_metadata` · Effect: **mutate-workspace** · Autonomy: **operator**

Update node metadata.

> Metadata carries the *Deterministic route flags (incl. publishExecutorDeterministic). This tool replaces the row's metadata field; the run-time overlay merges it per key with canonical.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "minLength": 1
    },
    "newId": {
      "type": "string",
      "minLength": 1
    },
    "node": {},
    "patch": {
      "type": "object"
    },
    "create": {
      "type": "array"
    },
    "update": {
      "type": "array"
    },
    "delete": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "dependencies": {
      "type": "object"
    },
    "orderedNodeIds": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "positions": {
      "type": "object"
    },
    "expectedWorkspaceVersion": {
      "type": "integer",
      "minimum": 0
    },
    "baseRevisionId": {
      "type": "string"
    },
    "actor": {
      "oneOf": [
        {
          "type": "string",
          "minLength": 1
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "enum": [
                "human",
                "agent",
                "system"
              ]
            },
            "id": {
              "type": "string"
            },
            "label": {
              "type": "string"
            }
          }
        }
      ]
    },
    "source": {
      "type": "string",
      "enum": [
        "mcp",
        "ui",
        "system"
      ]
    },
    "summary": {
      "type": "string"
    },
    "reason": {
      "type": "string"
    },
    "correlation": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "runId": {
          "type": "string"
        },
        "requestId": {
          "type": "string"
        }
      }
    }
  },
  "required": [],
  "additionalProperties": false
}
```

</details>

#### `workspace_update_node_model_config`

Internal name: `workspace.update_node_model_config` · Effect: **mutate-workspace** · Autonomy: **caution**

Update node modelConfig. Recursively MERGES the given keys onto the node's existing modelConfig — keys the patch omits are preserved, not dropped; a key present in the patch overwrites (nested plain objects merge key-by-key, any other value including arrays replaces outright).

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "minLength": 1
    },
    "newId": {
      "type": "string",
      "minLength": 1
    },
    "node": {},
    "patch": {
      "type": "object"
    },
    "create": {
      "type": "array"
    },
    "update": {
      "type": "array"
    },
    "delete": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "dependencies": {
      "type": "object"
    },
    "orderedNodeIds": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "positions": {
      "type": "object"
    },
    "expectedWorkspaceVersion": {
      "type": "integer",
      "minimum": 0
    },
    "baseRevisionId": {
      "type": "string"
    },
    "actor": {
      "oneOf": [
        {
          "type": "string",
          "minLength": 1
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "enum": [
                "human",
                "agent",
                "system"
              ]
            },
            "id": {
              "type": "string"
            },
            "label": {
              "type": "string"
            }
          }
        }
      ]
    },
    "source": {
      "type": "string",
      "enum": [
        "mcp",
        "ui",
        "system"
      ]
    },
    "summary": {
      "type": "string"
    },
    "reason": {
      "type": "string"
    },
    "correlation": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "runId": {
          "type": "string"
        },
        "requestId": {
          "type": "string"
        }
      }
    }
  },
  "required": [],
  "additionalProperties": false
}
```

</details>

#### `workspace_update_node_output_schema`

Internal name: `workspace.update_node_output_schema` · Effect: **mutate-workspace** · Autonomy: **caution**

Update node output JSON Schema draft 2020-12.

> Changes what the executor validates model output against; publisher.ts still validates article_body against the CANONICAL schema.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "minLength": 1
    },
    "schema": {
      "type": [
        "object",
        "boolean"
      ]
    },
    "expectedWorkspaceVersion": {
      "type": "integer",
      "minimum": 0
    },
    "baseRevisionId": {
      "type": "string"
    },
    "actor": {
      "oneOf": [
        {
          "type": "string",
          "minLength": 1
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "enum": [
                "human",
                "agent",
                "system"
              ]
            },
            "id": {
              "type": "string"
            },
            "label": {
              "type": "string"
            }
          }
        }
      ]
    },
    "source": {
      "type": "string",
      "enum": [
        "mcp",
        "ui",
        "system"
      ]
    },
    "summary": {
      "type": "string"
    },
    "reason": {
      "type": "string"
    },
    "correlation": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "runId": {
          "type": "string"
        },
        "requestId": {
          "type": "string"
        }
      }
    }
  },
  "required": [
    "id",
    "schema"
  ],
  "additionalProperties": false
}
```

</details>

#### `workspace_update_node_prompt`

Internal name: `workspace.update_node_prompt` · Effect: **mutate-workspace** · Autonomy: **caution**

Update a node prompt.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "minLength": 1
    },
    "prompt": {
      "type": "string",
      "minLength": 1
    },
    "expectedWorkspaceVersion": {
      "type": "integer",
      "minimum": 0
    },
    "baseRevisionId": {
      "type": "string"
    },
    "actor": {
      "oneOf": [
        {
          "type": "string",
          "minLength": 1
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "enum": [
                "human",
                "agent",
                "system"
              ]
            },
            "id": {
              "type": "string"
            },
            "label": {
              "type": "string"
            }
          }
        }
      ]
    },
    "source": {
      "type": "string",
      "enum": [
        "mcp",
        "ui",
        "system"
      ]
    },
    "summary": {
      "type": "string"
    },
    "reason": {
      "type": "string"
    },
    "correlation": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "runId": {
          "type": "string"
        },
        "requestId": {
          "type": "string"
        }
      }
    }
  },
  "required": [
    "id",
    "prompt"
  ],
  "additionalProperties": false
}
```

</details>

#### `workspace_update_node_skills`

Internal name: `workspace.update_node_skills` · Effect: **mutate-workspace** · Autonomy: **caution**

Update node assignedSkills.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "minLength": 1
    },
    "newId": {
      "type": "string",
      "minLength": 1
    },
    "node": {},
    "patch": {
      "type": "object"
    },
    "create": {
      "type": "array"
    },
    "update": {
      "type": "array"
    },
    "delete": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "dependencies": {
      "type": "object"
    },
    "orderedNodeIds": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "positions": {
      "type": "object"
    },
    "expectedWorkspaceVersion": {
      "type": "integer",
      "minimum": 0
    },
    "baseRevisionId": {
      "type": "string"
    },
    "actor": {
      "oneOf": [
        {
          "type": "string",
          "minLength": 1
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "enum": [
                "human",
                "agent",
                "system"
              ]
            },
            "id": {
              "type": "string"
            },
            "label": {
              "type": "string"
            }
          }
        }
      ]
    },
    "source": {
      "type": "string",
      "enum": [
        "mcp",
        "ui",
        "system"
      ]
    },
    "summary": {
      "type": "string"
    },
    "reason": {
      "type": "string"
    },
    "correlation": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "runId": {
          "type": "string"
        },
        "requestId": {
          "type": "string"
        }
      }
    }
  },
  "required": [],
  "additionalProperties": false
}
```

</details>

#### `workspace_update_node_tools`

Internal name: `workspace.update_node_tools` · Effect: **mutate-workspace** · Autonomy: **operator**

Update node allowedTools.

> Changing tool grants on a publish/admin node is an operator decision (canonical publish nodes already carry project.call_tool); this tool records only change history as a guard.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "minLength": 1
    },
    "newId": {
      "type": "string",
      "minLength": 1
    },
    "node": {},
    "patch": {
      "type": "object"
    },
    "create": {
      "type": "array"
    },
    "update": {
      "type": "array"
    },
    "delete": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "dependencies": {
      "type": "object"
    },
    "orderedNodeIds": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "positions": {
      "type": "object"
    },
    "expectedWorkspaceVersion": {
      "type": "integer",
      "minimum": 0
    },
    "baseRevisionId": {
      "type": "string"
    },
    "actor": {
      "oneOf": [
        {
          "type": "string",
          "minLength": 1
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "enum": [
                "human",
                "agent",
                "system"
              ]
            },
            "id": {
              "type": "string"
            },
            "label": {
              "type": "string"
            }
          }
        }
      ]
    },
    "source": {
      "type": "string",
      "enum": [
        "mcp",
        "ui",
        "system"
      ]
    },
    "summary": {
      "type": "string"
    },
    "reason": {
      "type": "string"
    },
    "correlation": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "runId": {
          "type": "string"
        },
        "requestId": {
          "type": "string"
        }
      }
    }
  },
  "required": [],
  "additionalProperties": false
}
```

</details>

#### `workspace_update_relationships`

Internal name: `workspace.update_relationships` · Effect: **mutate-workspace** · Autonomy: **caution**

Create, update, or delete typed constellation relationships (data/memory/policy/evaluation/approval). Execution edges are derived from node.dependsOn and cannot be stored.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "create": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "kind",
          "sourceId",
          "targetId"
        ],
        "properties": {
          "id": {
            "type": "string"
          },
          "kind": {
            "type": "string",
            "enum": [
              "execution",
              "data",
              "memory",
              "policy",
              "evaluation",
              "approval"
            ]
          },
          "sourceId": {
            "type": "string"
          },
          "targetId": {
            "type": "string"
          },
          "direction": {
            "type": "string",
            "enum": [
              "forward",
              "bidirectional"
            ]
          },
          "label": {
            "type": "string"
          },
          "enabled": {
            "type": "boolean"
          },
          "metadata": {
            "type": "object"
          },
          "schemaRefs": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "artifactRefs": {
            "type": "array",
            "items": {
              "type": "string"
            }
          }
        }
      }
    },
    "update": {
      "type": "array",
      "items": {
        "type": "object"
      }
    },
    "delete": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "expectedWorkspaceVersion": {
      "type": "integer",
      "minimum": 0
    },
    "baseRevisionId": {
      "type": "string"
    },
    "actor": {
      "oneOf": [
        {
          "type": "string",
          "minLength": 1
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "enum": [
                "human",
                "agent",
                "system"
              ]
            },
            "id": {
              "type": "string"
            },
            "label": {
              "type": "string"
            }
          }
        }
      ]
    },
    "source": {
      "type": "string",
      "enum": [
        "mcp",
        "ui",
        "system"
      ]
    },
    "summary": {
      "type": "string"
    },
    "reason": {
      "type": "string"
    },
    "correlation": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "runId": {
          "type": "string"
        },
        "requestId": {
          "type": "string"
        }
      }
    }
  },
  "required": [],
  "additionalProperties": false
}
```

</details>

#### `workspace_validate_graph`

Internal name: `workspace.validate_graph` · Effect: **read** · Autonomy: **safe**

Validate workflow graph: ids, statuses, risk levels, missing dependencies, cycles, publish-chain edges, and (R-21) that every conductor-sequence node's dependsOn / requiredInputs entries are actually satisfiable by the conductor sequence.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {},
  "required": [],
  "additionalProperties": false
}
```

</details>

#### `workspace_validate_node`

Internal name: `workspace.validate_node` · Effect: **read** · Autonomy: **safe**

Validate a node or existing node id.

<details><summary>Input schema</summary>

```json
{
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "minLength": 1
    },
    "newId": {
      "type": "string",
      "minLength": 1
    },
    "node": {},
    "patch": {
      "type": "object"
    },
    "create": {
      "type": "array"
    },
    "update": {
      "type": "array"
    },
    "delete": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "dependencies": {
      "type": "object"
    },
    "orderedNodeIds": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "positions": {
      "type": "object"
    },
    "expectedWorkspaceVersion": {
      "type": "integer",
      "minimum": 0
    },
    "baseRevisionId": {
      "type": "string"
    },
    "actor": {
      "oneOf": [
        {
          "type": "string",
          "minLength": 1
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "type": "string",
              "enum": [
                "human",
                "agent",
                "system"
              ]
            },
            "id": {
              "type": "string"
            },
            "label": {
              "type": "string"
            }
          }
        }
      ]
    },
    "source": {
      "type": "string",
      "enum": [
        "mcp",
        "ui",
        "system"
      ]
    },
    "summary": {
      "type": "string"
    },
    "reason": {
      "type": "string"
    },
    "correlation": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "runId": {
          "type": "string"
        },
        "requestId": {
          "type": "string"
        }
      }
    }
  },
  "required": [],
  "additionalProperties": false
}
```

</details>

## Output envelope

Success: `{ "ok": true, "data": <result> }` inside the MCP `structuredContent` (and JSON-stringified in `content[0].text`). Failure: a JSON-RPC error `{ code: -32603, message: "<code>: <summary>", data: { ok: false, error: { code, message?, issues? } } }` — see `toolError` / `toolErrorSummary` in `src/agent/mcp/workspace/toolKit.ts`. Unknown tool: `-32602`. Output schemas are not declared per tool on the wire (no `outputSchema` in `tools/list`); the TypeScript return types in the tool modules are the only contract.
